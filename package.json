{
  "name": "dgdash-racing-system",
  "version": "1.0.0",
  "type": "module",
  "description": "DGDash Racing System (Cyberpunk / Neo-Racing UI)",
  "main": "server/index.js",
  "scripts": {
    "dev:server": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:client",
    "start": "node server/index.js",
    "import:roster": "node scripts/import-roster.js",
    "test": "node server/tests/race-flow.test.js && node server/tests/camera-scanner.test.js && node server/tests/coupon-package.test.js && node server/tests/marshal-flow.test.js && node server/tests/ticket-engine-flow.test.js && node server/tests/bracket-dashboard-render.test.js && node server/tests/bracket-3lane.test.js && node server/tests/e2e-tournament-lifecycle.test.js && node server/tests/migration-foundation.test.js && node server/tests/participant-registration.test.js && node server/tests/bto-service.test.js && node server/tests/winner-registration.test.js && node server/tests/state-contract.test.js && node server/tests/round-lock.test.js && node server/tests/multi-round-winner-registration.test.js && node server/tests/qualifying-lock-manual-solo-run.test.js && node server/tests/bracket-reset-heat.test.js && node server/tests/auth-service.test.js && node server/tests/admin-approval-rbac.test.js && node server/tests/reproduce-auth-google-payload.test.js && node server/tests/google-sheet-sync.test.js && node server/tests/phase-27-mutation-rbac.test.js && node --test server/tests/phase-28-enhancements.test.js",
    "docker:build": "docker compose build",
    "docker:rebuild": "docker compose up -d --build",
    "docker:watch": "docker compose watch",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:restart": "docker compose restart",
    "setup:hooks": "node scripts/setup-git-hooks.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-auth-library": "^11.1.0",
    "socket.io": "^4.8.1",
    "sql.js": "^1.14.2",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
