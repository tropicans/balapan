---
phase: "09-finish-to-next-round-ticket-engine-multi-round-bracket-integ"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "server/db.js"
  - "server/ticketEngine.js"
  - "server/raceManager.js"
  - "server/index.js"
  - "server/tests/ticket-engine-flow.test.js"
  - "package.json"
autonomous: true
requirements:
  - "TKET-01"
  - "TKET-02"
  - "TKET-03"

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Tabel next_round_tickets dibuat di SQLite untuk mencatat nomor tiket sequential (1, 2, 3...), kode tiket TKT-B2-XXX, user_id, racer_ticket_index, serial_number kupon, dan referensi slot bracket match."
    - "Tabel bracket_matches dimodifikasi dengan kolom ticket_id_1, ticket_id_2, ticket_id_3, dan is_auto_advanced."
    - "Service terpusat TicketEngine.issueTicket() menerbitkan tiket Babak 2 secara atomic, mendukung multi-tiket per pembalap dengan label 'Nama Pembalap #X', dan menempatkan tiket secara berurutan ke slot kosong Round 2."
    - "Aturan All-3-Same-Lane Auto-Advance secara otomatis menandai match Round 2 sebagai selesai (status: 'completed', is_auto_advanced: 1) jika ketiga slot terisi oleh pembalap yang sama, serta meloloskan pembalap ke Round berikutnya."
    - "Pintu masuk meja finish /marshal (POST /api/marshal/record-winner) dan Race Director (RaceManager.submitFinishTimes) terintegrasi ke TicketEngine.issueTicket()."
    - "Pembatalan kemenangan (POST /api/marshal/undo-last-winner) menandai tiket sebagai void, mengosongkan kembali slot pada bracket match, dan mengembalikan kuota kupon fisik."
    - "Mobil yang dinyatakan DNF / Klontang (All CO) hangus kuponnya tanpa penerbitan record tiket apa pun (zero ticket)."
  artifacts:
    - "server/db.js"
    - "server/ticketEngine.js"
    - "server/raceManager.js"
    - "server/index.js"
    - "server/tests/ticket-engine-flow.test.js"
  key_links:
    - "server/ticketEngine.js -> server/db.js: Menggunakan db.transaction() untuk atomic ticket number increment, slot assignment, dan auto-advance status."
    - "server/index.js -> server/ticketEngine.js: Endpoint /api/marshal/record-winner dan undo memanggil TicketEngine.issueTicket() dan TicketEngine.voidTicket()."
    - "server/raceManager.js -> server/ticketEngine.js: Scrutineer pass memanggil TicketEngine.issueTicket()."
    - "server/tests/ticket-engine-flow.test.js -> server/ticketEngine.js: Test suite memvalidasi seluruh flow multi-ticket, auto-placement, auto-advance, dan void rollback."
---

<objective>
Membangun fondasi backend Ticket Engine (`server/ticketEngine.js`) dan skema database `next_round_tickets` untuk menerbitkan tiket Babak 2 secara otomatis dan sequential (`#1`, `#2` / `TKT-B2-001`), mendukung tradisi Mini 4WD multi-tiket per pembalap (`Budi #1`, `Budi #2`), melakukan auto-seeding ke slot kosong Babak 2 pada bagan eliminasi 3-jalur (`bracket_matches`), mengeksekusi aturan All-3-Same-Lane Auto-Advance, serta mengintegrasikan dual-source trigger (`/marshal` & `RaceDirector`) dan penanganan hangus kupon DNF/CO tanpa tiket.

Output: Skema database tiket, modul `TicketEngine.js`, integrasi backend, dan test suite Wave 0 `ticket-engine-flow.test.js` yang 100% lulus.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-finish-to-next-round-ticket-engine-multi-round-bracket-integ/09-CONTEXT.md
@.planning/phases/09-finish-to-next-round-ticket-engine-multi-round-bracket-integ/09-RESEARCH.md
@.planning/phases/09-finish-to-next-round-ticket-engine-multi-round-bracket-integ/09-VALIDATION.md
@server/db.js
@server/index.js
@server/raceManager.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Skema Database next_round_tickets, Migrasi Kolom bracket_matches, dan Test Suite Wave 0</name>
  <files>server/db.js, server/tests/ticket-engine-flow.test.js, package.json</files>
  <read_first>server/db.js, server/tests/marshal-flow.test.js, package.json</read_first>
  <threat_model>
    <threat>T-09-01: Inkonsistensi data tiket, benturan UNIQUE constraint pada tiket sequential saat concurrent writes, atau kegagalan migrasi kolom bracket_matches.</threat>
    <mitigation>Gunakan skema SQLite dengan PRIMARY KEY UUID, UNIQUE ticket_number & ticket_code, index pada user_id dan serial_number, serta migrasi ALTER TABLE defensif dengan try/catch.</mitigation>
  </threat_model>
  <behavior>
    - Test 1: Inisialisasi database membuat tabel `next_round_tickets` dengan kolom: id, ticket_number, ticket_code, user_id, racer_ticket_index, package_id, serial_number, lane, source, status, bracket_match_id, bracket_slot, void_reason, created_at, updated_at.
    - Test 2: Tabel `bracket_matches` memiliki kolom baru `ticket_id_1`, `ticket_id_2`, `ticket_id_3`, dan `is_auto_advanced`.
    - Test 3: Relasi foreign key ke users, coupon_packages, dan bracket_matches terverifikasi.
  </behavior>
  <action>
    1. Buat test suite Wave 0 di `server/tests/ticket-engine-flow.test.js` menggunakan built-in `node:assert` yang memverifikasi keberadaan tabel `next_round_tickets`, kolom-kolomnya, dan kolom baru di `bracket_matches`.
    2. Perbarui `package.json` pada script `"test"` untuk menyertakan eksekusi `node server/tests/ticket-engine-flow.test.js`.
    3. Di `server/db.js`, tambahkan skema `next_round_tickets` dalam `initDatabase()`:
       ```sql
       CREATE TABLE IF NOT EXISTS next_round_tickets (
         id TEXT PRIMARY KEY,
         ticket_number INTEGER UNIQUE NOT NULL,
         ticket_code TEXT UNIQUE NOT NULL,
         user_id TEXT NOT NULL,
         racer_ticket_index INTEGER NOT NULL DEFAULT 1,
         package_id TEXT NOT NULL,
         serial_number TEXT NOT NULL,
         lane TEXT CHECK(lane IN ('A', 'B', 'C')) NOT NULL,
         source TEXT CHECK(source IN ('marshal', 'race_director')) DEFAULT 'marshal',
         status TEXT CHECK(status IN ('issued', 'used', 'void')) DEFAULT 'issued',
         bracket_match_id TEXT,
         bracket_slot TEXT CHECK(bracket_slot IN ('user_id_1', 'user_id_2', 'user_id_3')),
         void_reason TEXT DEFAULT NULL,
         created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
         updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
         FOREIGN KEY(user_id) REFERENCES users(id),
         FOREIGN KEY(package_id) REFERENCES coupon_packages(id),
         FOREIGN KEY(bracket_match_id) REFERENCES bracket_matches(id)
       );

       CREATE INDEX IF NOT EXISTS idx_tickets_user ON next_round_tickets(user_id);
       CREATE INDEX IF NOT EXISTS idx_tickets_serial ON next_round_tickets(serial_number);
       CREATE INDEX IF NOT EXISTS idx_tickets_status ON next_round_tickets(status);
       ```
    4. Tambahkan migrasi kolom `bracket_matches` di `server/db.js`:
       ```javascript
       try { db.exec(`ALTER TABLE bracket_matches ADD COLUMN ticket_id_1 TEXT;`); } catch (e) {}
       try { db.exec(`ALTER TABLE bracket_matches ADD COLUMN ticket_id_2 TEXT;`); } catch (e) {}
       try { db.exec(`ALTER TABLE bracket_matches ADD COLUMN ticket_id_3 TEXT;`); } catch (e) {}
       try { db.exec(`ALTER TABLE bracket_matches ADD COLUMN is_auto_advanced INTEGER DEFAULT 0;`); } catch (e) {}
       ```
  </action>
  <verify>
    <automated>node server/tests/ticket-engine-flow.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `server/db.js` contains `CREATE TABLE IF NOT EXISTS next_round_tickets`
    - `server/db.js` contains alter statements for `ticket_id_1` and `is_auto_advanced`
    - `server/tests/ticket-engine-flow.test.js` exits with code 0 for Task 1 assertions
  </acceptance_criteria>
  <done>Skema tabel `next_round_tickets` dan ekstensi kolom `bracket_matches` aktif di SQLite dan diverifikasi test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service Terpusat TicketEngine — Sequential Issuing, Multi-Ticket Labeling, Auto-Seeding, dan All-3-Same-Lane Auto-Advance</name>
  <files>server/ticketEngine.js, server/raceManager.js, server/tests/ticket-engine-flow.test.js</files>
  <read_first>server/ticketEngine.js, server/raceManager.js, server/db.js, server/tests/ticket-engine-flow.test.js</read_first>
  <threat_model>
    <threat>T-09-02: Race condition auto-seeding ke slot yang sama saat heat selesai berdekatan, atau kegagalan mendeteksi All-3-Same-Lane auto-advance.</threat>
    <mitigation>Seluruh alokasi nomor tiket sequential dan penempatan slot Round 2 dieksekusi di dalam db.transaction() yang serial; validasi ketiga slot setelah penempatan.</mitigation>
  </threat_model>
  <behavior>
    - Test 1: TicketEngine.issueTicket() menerbitkan tiket dengan ticket_number incremental sequential (1, 2, 3...) dan ticket_code TKT-B2-001.
    - Test 2: Jika pembalap menang berkali-kali (D-01), setiap tiket mencatat racer_ticket_index (1, 2, 3...) dan menghasilkan label format 'Budi #1', 'Budi #2' (D-03).
    - Test 3: Tiket otomatis ditempatkan ke slot kosong Round 2 di tabel bracket_matches (user_id_1 -> user_id_2 -> user_id_3) dan mengisi ticket_id_X (D-02).
    - Test 4: Jika 3 tiket dari pembalap yang sama mengisi seluruh 3 jalur pada satu heat Round 2, sistem menandai match tersebut: status = 'completed', is_auto_advanced = 1, winner_id = userId, dan secara otomatis memajukan pembalap ke Round berikutnya (D-04).
  </behavior>
  <action>
    1. Buat file baru `server/ticketEngine.js`:
       - Import `db` dan `uuidv4`.
       - Class `TicketEngine`:
         * `static issueTicket({ userId, packageId, serialNumber, lane, source = 'marshal', heatNumber = null })`:
           - Eksekusi dalam `db.transaction()`:
             * Ambil `ticket_number`: `SELECT COALESCE(MAX(ticket_number), 0) + 1 as next_num FROM next_round_tickets`.
             * Format `ticket_code`: `TKT-B2-${String(next_num).padStart(3, '0')}`.
             * Hitung `racer_ticket_index`: `SELECT COUNT(*) as count FROM next_round_tickets WHERE user_id = ? AND status != 'void'` + 1.
             * Cari open slot di Round 2 (`bracket_matches` dengan `round_number = 2` yang memiliki slot kosong `user_id_1`, `user_id_2`, atau `user_id_3` diurutkan `match_number ASC`).
             * Jika tidak ada open slot, buat heat baru di Round 2.
             * Assign `user_id_X = userId` dan `ticket_id_X = ticketId` pada match tersebut. Catat `bracket_match_id` dan `bracket_slot` ('user_id_1', 'user_id_2', atau 'user_id_3').
             * Insert ke `next_round_tickets`.
             * Cek aturan **All-3-Same-Lane Auto-Advance (D-04)**: Ambil kembali match Round 2 tersebut. Jika `m.user_id_1 === m.user_id_2 && m.user_id_2 === m.user_id_3 && m.user_id_1 === userId`:
               - Update match: `status = 'completed'`, `is_auto_advanced = 1`, `winner_id = userId`.
               - Panggil `RaceManager.advanceBracketWinner(m.id, userId)` untuk meloloskan 1 slot pembalap ke round berikutnya (Round 3 / Grand Final).
               - Tandai flag `autoAdvanced: true` pada hasil kembalian.
             * Return object tiket lengkap termasuk `racer_label: "${userName} #${racer_ticket_index}"`.
         * `static voidTicket(ticketId, reason = 'Marshal undo')`:
           - Batalkan tiket dalam `db.transaction()`:
             * Update `next_round_tickets`: `status = 'void'`, `void_reason = reason`.
             * Jika tiket terhubung ke `bracket_match_id` dan `bracket_slot`: kosongkan slot tersebut di `bracket_matches` (`user_id_X = NULL`, `ticket_id_X = NULL`). Jika match sebelumnya `is_auto_advanced = 1`, kembalikan `status = 'pending'`, `is_auto_advanced = 0`, `winner_id = NULL`.
             * Kembalikan sisa kuota kupon di `coupon_packages` (+1) dan saldo `coupons` (+1).
         * `static getTicketStats()`:
           - Mengembalikan total tiket terbit aktif, total tiket void, dan daftar pembalap dengan jumlah tiketnya.
    2. Tambahkan pengujian di `server/tests/ticket-engine-flow.test.js` yang memvalidasi issuance berurutan, penamaan multi-tiket, penempatan slot bracket, dan eksekusi All-3-Same-Lane auto-advance.
  </action>
  <verify>
    <automated>node server/tests/ticket-engine-flow.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `server/ticketEngine.js` exists and exports `TicketEngine`
    - `TicketEngine.issueTicket()` returns sequential `ticket_number` and proper `racer_ticket_index`
    - All-3-Same-Lane condition sets `is_auto_advanced = 1` and calls `advanceBracketWinner`
    - Test suite passes Task 2 assertions
  </acceptance_criteria>
  <done>Service `TicketEngine` beroperasi penuh dengan dukungan multi-ticket sequential dan All-3-Same-Lane auto-advance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrasi Dual-Source Trigger (/marshal & RD), Void Rollback 60s, dan Jaminan DNF/CO Zero-Ticket</name>
  <files>server/index.js, server/raceManager.js, server/ticketEngine.js, server/tests/ticket-engine-flow.test.js</files>
  <read_first>server/index.js, server/raceManager.js, server/ticketEngine.js, server/tests/ticket-engine-flow.test.js</read_first>
  <threat_model>
    <threat>T-09-03: Penerbitan tiket ganda antara alur Marshal dan Race Director, atau tiket terbit saat mobil mengalami Crash Out (CO) / DNF.</threat>
    <mitigation>Konsolidasi panggilan penerbitan tiket ke TicketEngine.issueTicket(); verifikasi status DNF di RaceManager tidak memanggil issueTicket().</mitigation>
  </threat_model>
  <behavior>
    - Test 1: POST /api/marshal/record-winner memanggil TicketEngine.issueTicket(), mengembalikan payload tiket Babak 2 lengkap (nomor tiket #X, kode tiket, slot bracket).
    - Test 2: POST /api/marshal/undo-last-winner dalam 60s memanggil TicketEngine.voidTicket() yang mengosongkan slot bracket dan mengembalikan kuota kupon.
    - Test 3: RaceManager.submitFinishTimes dan Scrutineer pass menerbitkan tiket Babak 2 via TicketEngine.
    - Test 4: RaceManager.declareAllCO() dan mobil dengan finish_time null/CO tidak menerbitkan tiket di next_round_tickets (TKET-03).
  </behavior>
  <action>
    1. Di `server/index.js`:
       - Perbarui endpoint `POST /api/marshal/record-winner`:
         * Gantikan pemanggilan langsung `RaceManager.seedIntoBracket(pkg.user_id)` dengan `TicketEngine.issueTicket({ userId: pkg.user_id, packageId: pkg.id, serialNumber: pkg.serial_number, lane, source: 'marshal', heatNumber: heat_number || null })`.
         * Sertakan info tiket pada respons `data` dan WebSocket payload `io.emit('ticket:granted', ...)` serta `io.emit('marshal:winner-recorded', ...)`.
       - Perbarui endpoint `POST /api/marshal/undo-last-winner`:
         * Cari tiket aktif terakhir terkait log ini di `next_round_tickets`.
         * Panggil `TicketEngine.voidTicket(ticket.id, 'Marshal 60s Undo')`.
         * Emit WebSocket `io.emit('ticket:voided', { ticketId: ticket.id, ticketNumber: ticket.ticket_number })`.
    2. Di `server/raceManager.js`:
       - Pada method `handleScrutineerAction` saat `action === 'pass'`:
         * Panggil `TicketEngine.issueTicket({ userId: reg.user_id, packageId: pkg?.id || null, serialNumber: pkg?.serial_number || 'DIGITAL', lane: reg.lane, source: 'race_director', heatNumber: reg.race_id })`.
       - Pada method `declareAllCO` dan penanganan finish tanpa pemenang:
         * Pastikan tidak ada pemanggilan `TicketEngine.issueTicket()`. Kupon tetap terpotong, kuota tetap berkurang, status race selesai, dan `next_round_tickets` tidak bertambah.
    3. Perluas test suite `server/tests/ticket-engine-flow.test.js` untuk menguji integrasi API marshal record-winner, undo-last-winner, dan skenario DNF zero-ticket.
  </action>
  <verify>
    <automated>node server/tests/ticket-engine-flow.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `POST /api/marshal/record-winner` returns ticket info in response
    - `POST /api/marshal/undo-last-winner` marks ticket as void and frees bracket slot
    - `declareAllCO()` leaves `next_round_tickets` count unchanged
    - Test suite passes all Task 3 integration tests
  </acceptance_criteria>
  <done>Integrasi dual-source trigger, void rollback 60 detik, dan penanganan DNF zero-ticket tervalidasi dan siap digunakan.</done>
</task>

</tasks>
