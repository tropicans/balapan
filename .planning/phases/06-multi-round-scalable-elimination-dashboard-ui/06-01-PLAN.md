---
phase: 06-multi-round-scalable-elimination-dashboard-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ui/Navbar.jsx
  - client/src/screens/BracketDashboard.jsx
  - client/src/context/RaceContext.jsx
autonomous: true
requirements:
  - ELIM-04
  - ELIM-05
  - ELIM-06
  - ELIM-07
must_haves:
  truths:
    - "Tab navigasi utama di Navbar menampilkan label 'Babak Eliminasi' menggantikan 'Bracket Babak 2' (ELIM-07)"
    - "BracketDashboard menampilkan Round Selector Tabs dinamis (Babak 2, Babak 3, Babak 4, Grand Final) dengan counter jumlah heat aktif dan selesai (ELIM-04)"
    - "Setiap kartu pertandingan menampilkan 3 baris pembalap ber-aksen warna jalur resmi: Line A Merah/Pink, Line B Cyan/Biru, Line C Hijau dengan tombol MENANG dan mahkota juara (ELIM-05)"
    - "Tersedia input pencarian instan (nomor heat / nama / tim pembalap) dan pagination cyberpunk 12-item per halaman untuk navigasi mulus 100+ heat (ELIM-06)"
    - "Kompilasi frontend Vite production dan seluruh test backend lulus 100% tanpa error atau regresi"
  artifacts:
    - client/src/components/ui/Navbar.jsx
    - client/src/screens/BracketDashboard.jsx
    - client/src/context/RaceContext.jsx
---

<objective>
Merombak antarmuka dasbor turnamen eliminasi (`BracketDashboard.jsx`) dan navigasi `Navbar.jsx` untuk mendukung turnamen berjenjang multi-babak (Babak 2 s.d. Grand Final), kartu pertandingan 3-jalur (Line A Pink, Line B Cyan, Line C Green), serta filter pencarian instan dan pagination ergonomis untuk 100+ heat.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update Navbar Navigation Tab (ELIM-07)</name>
  <files>client/src/components/ui/Navbar.jsx</files>
  <action>
    1. Di `client/src/components/ui/Navbar.jsx`:
       - Pada array `screens`, perbarui item ber-id `'bracket'`:
         - Ubah `label` dari `'Bracket Babak 2'` menjadi `'Babak Eliminasi'`.
         - Pertahankan `id: 'bracket'`, icon `GitBranch`, dan warna `pink` agar konsisten dengan sistem routing internal.
  </action>
  <verify>npm --prefix client run build</verify>
  <done>Tab navbar menampilkan 'Babak Eliminasi' dengan build sukses</done>
</task>

<task type="auto">
  <name>Task 2: Enhance apiAdvanceBracket in RaceContext.jsx</name>
  <files>client/src/context/RaceContext.jsx</files>
  <action>
    1. Di `client/src/context/RaceContext.jsx`:
       - Perbarui fungsi `apiAdvanceBracket(matchId, winnerId, options = {})` agar menerima argumen `options` (seperti `{ isFinal: true }`) dan mengirimkannya dalam body request POST ke `/api/bracket/advance`.
       - Pastikan error message ditangkap dan dilempar secara bersih jika backend mengembalikan status error.
  </action>
  <verify>npm --prefix client run build</verify>
  <done>RaceContext siap mengirim parameter promosi pemenang dan flag Grand Final ke backend</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dynamic Round Selector Tabs in BracketDashboard.jsx (ELIM-04)</name>
  <files>client/src/screens/BracketDashboard.jsx</files>
  <action>
    1. Di `client/src/screens/BracketDashboard.jsx`:
       - Ekstrak daftar unik `round_number` dari `raceState.bracketMatches || []`.
       - Jika tidak ada ronde ditemukan, default ke `[2, 3]`.
       - Sediakan state `selectedRound` (default ke ronde terendah yang ada, misal `2`).
       - Tampilkan bar navigasi tab horizontal ber-aksen Cyberpunk:
         - Format nama ronde:
           - Ronde 2: `BABAK 2 // PENYISIHAN 3-JALUR`
           - Ronde 3+: `BABAK R // PEREMPAT FINAL / SEMIFINAL` (atau `GRAND FINAL // KEJUARAAN` untuk ronde tertinggi jika match memiliki `is_final` atau merupakan match puncak).
         - Sertakan badge ringkasan pada tiap tab: `{total} Heat • {selesai} Selesai`.
         - Tombol tab aktif menggunakan neon border glowing (`border-neonCyan text-white shadow-glowCyan`).
  </action>
  <verify>npm --prefix client run build</verify>
  <done>Round Selector Tabs dinamis terpasang dan dapat diklik untuk beralih babak</done>
</task>

<task type="auto">
  <name>Task 4: Implement 3-Lane Heat Cards & Champion Crowning (ELIM-05)</name>
  <files>client/src/screens/BracketDashboard.jsx</files>
  <action>
    1. Di `client/src/screens/BracketDashboard.jsx` pada `renderMatchCard`:
       - Rombak kartu pertandingan agar mendukung 3 baris jalur balap:
         a. **Line A**: Border `border-neonPink/40 bg-neonPink/5`, label badge pink `LINE A`, nama racer `match.user_1_name` & tim `match.user_1_team`.
         b. **Line B**: Border `border-neonCyan/40 bg-neonCyan/5`, label badge cyan `LINE B`, nama racer `match.user_2_name` & tim `match.user_2_team`.
         c. **Line C**: Border `border-neonGreen/40 bg-neonGreen/5`, label badge green `LINE C`, nama racer `match.user_3_name` & tim `match.user_3_team`.
       - Jika slot kosong, tampilkan teks cyber placeholder: `Menunggu Pembalap...`
       - Tombol **"MENANG"**:
         - Muncul pada kontestan yang terisi jika match belum selesai (`match.status !== 'completed'`).
         - Saat diklik, memanggil `apiAdvanceBracket(match.id, userId, { isFinal: match.is_final === 1 })`.
       - Indikator Pemenang:
         - Kontestan pemenang (`winner_id === userId`) di-highlight dengan border neon hijau, badge `"LOLOS / JUARA"`, dan ikon `Crown` emas/hijau.
       - Header kartu menampilkan: `MATCH #{match.match_number}` dan status `PENDING` (amber) / `SELESAI` (green).
  </action>
  <verify>npm --prefix client run build</verify>
  <done>Kartu heat menampilkan 3 jalur warna resmi dengan tombol Menang dan status juara yang jelas</done>
</task>

<task type="auto">
  <name>Task 5: Implement Search Filter, Status Filter & Pagination for 100+ Heats (ELIM-06)</name>
  <files>client/src/screens/BracketDashboard.jsx</files>
  <action>
    1. Di `client/src/screens/BracketDashboard.jsx`:
       - Tambahkan toolbar filter di atas grid heat:
         - **Input Search**: Field input teks dengan ikon `Search` untuk memfilter berdasarkan nomor heat (misal `"15"`), nama pembalap (`"Andi"`), atau nama tim (`"RRT"`).
         - **Filter Status**: Tombol toggle filter (`SEMUA`, `PENDING`, `SELESAI`).
         - **Quick Stats Bar**: Menampilkan total heat di babak terpilih, jumlah selesai, dan sisa pertandingan.
       - **Pagination Cyberpunk**:
         - Batasi tampilan ke 12 heat per halaman (`ITEMS_PER_PAGE = 12`).
         - Tombol navigasi halaman: `Halaman Sebelumnya`, `Halaman X dari Y`, `Halaman Berikutnya`.
         - Reset ke halaman 1 saat user berpindah tab babak atau mengetikkan query pencarian.
       - Tampilan Empty State: Jika pencarian tidak menemukan hasil, tampilkan kartu peringatan cyberpunk informatif.
  </action>
  <verify>npm --prefix client run build</verify>
  <done>Filter pencarian real-time dan pagination 12-heat aktif dan responsif</done>
</task>

<task type="auto">
  <name>Task 6: Build Verification & Regression Validation</name>
  <files>client/src/components/ui/Navbar.jsx, client/src/screens/BracketDashboard.jsx, client/src/context/RaceContext.jsx</files>
  <action>
    1. Jalankan kompilasi bundle Vite client:
       `npm --prefix client run build`
    2. Jalankan test suite backend eliminasi 3-jalur:
       `node server/tests/bracket-3lane.test.js`
    3. Jalankan test suite flow balapan menyeluruh:
       `node server/tests/race-flow.test.js`
  </action>
  <verify>
    npm --prefix client run build
    & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/bracket-3lane.test.js
    & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/race-flow.test.js
  </verify>
  <done>Seluruh pengujian kompilasi UI dan alur backend lulus 100% tanpa regresi</done>
</task>
</tasks>
