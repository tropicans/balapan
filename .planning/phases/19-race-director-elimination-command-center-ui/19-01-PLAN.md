---
phase: "19"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - client/src/context/RaceContext.jsx
  - client/src/components/director/EliminationManager.jsx
  - client/src/screens/RaceDirectorDashboard.jsx
autonomous: true
requirements: [RDELIM-01, RDELIM-02, RDELIM-03, RDELIM-04, RDELIM-05]
user_setup: []

must_haves:
  truths:
    - "Race Director dashboard /director displays integrated 3-lane Elimination Command Center for Babak 2 up to Grand Final, replacing obsolete digital race controls (RDELIM-01, D-01)"
    - "Race Director can select a heat winner with 1-click on contestant lane without popups, marking heat as completed and auto-advancing winner (RDELIM-02, D-05)"
    - "Heat cards visually differentiate completed heats (neon green border, SELESAI badge, JUARA crown) from pending heats (amber border, PENDING badge) (RDELIM-03, D-06)"
    - "Round 2 progress banner displays counter (X / Y Heat Selesai) and illuminates 'FINALISASI / KUNCI BABAK 2' button when 100% of heats are completed (RDELIM-03, RDELIM-04, D-03)"
    - "Clicking 'FINALISASI / KUNCI BABAK 2' opens confirmation modal and safely locks Round 2, freezing winner modifications (RDELIM-04, D-04)"
    - "When Round 2 is locked, 'BUKA KUNCI BABAK 2' emergency unlock button and modal are available for corrections (RDELIM-04, D-04)"
    - "All heat updates and round lock/unlock events synchronize in real-time across /director, Circuit TV, and /bracket via WebSocket (RDELIM-05)"
    - "Client build passes with zero Vite compilation errors and full test suite remains 100% green"
  artifacts:
    - path: "client/src/context/RaceContext.jsx"
      provides: "apiLockRound and apiUnlockRound methods, and Socket.IO listeners for round2:locked and round2:unlocked"
    - path: "client/src/components/director/EliminationManager.jsx"
      provides: "Dedicated Elimination Command Center component with round tabs, Round 2 progress toolbar, lock/unlock modals, 1-click winner selection, and heat search/filter"
    - path: "client/src/screens/RaceDirectorDashboard.jsx"
      provides: "Modernized Race Director dashboard hosting EliminationManager and BtoManager, with obsolete digital racing controls cleaned up"
  key_links:
    - from: "client/src/components/director/EliminationManager.jsx"
      to: "client/src/context/RaceContext.jsx"
      via: "useRace() hook for raceState.bracketMatches, apiAdvanceBracket, apiLockRound, apiUnlockRound"
      pattern: "useRace\\(\\)"
    - from: "client/src/screens/RaceDirectorDashboard.jsx"
      to: "client/src/components/director/EliminationManager.jsx"
      via: "Rendered under primary tab PUSAT KOMANDO ELIMINASI"
      pattern: "<EliminationManager"
---

<objective>
Build the Race Director Elimination Command Center in the React frontend, replacing obsolete digital race controls on `/director` with an integrated 3-lane bracket management interface, 1-click instant winner selection, visual heat completion indicators, and an interactive "Finalisasi / Kunci Babak 2" workflow with emergency unlock.

Purpose: Equip the Race Director with a clean, responsive command desk dedicated to managing the physical tournament from Babak 2 all the way to the Grand Final.
Output: Updates to `client/src/context/RaceContext.jsx`, new component `client/src/components/director/EliminationManager.jsx`, and refactored `client/src/screens/RaceDirectorDashboard.jsx`.
Citations: Fulfills RDELIM-01, RDELIM-02, RDELIM-03, RDELIM-04, and RDELIM-05, implementing decisions D-01 through D-08 from 19-CONTEXT.md.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-race-director-elimination-command-center-ui/19-CONTEXT.md
@.planning/phases/19-race-director-elimination-command-center-ui/19-UI-SPEC.md
@client/src/context/RaceContext.jsx
@client/src/screens/RaceDirectorDashboard.jsx
@client/src/screens/BracketDashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Round Lock/Unlock Methods and WebSocket Listeners to RaceContext</name>
  <files>client/src/context/RaceContext.jsx</files>
  <read_first>
    - client/src/context/RaceContext.jsx
    - .planning/phases/18-backend-services-round-2-finalization-state-contract/18-01-SUMMARY.md
  </read_first>
  <behavior>
    - apiLockRound(round = 2) sends POST /api/bracket/lock-round with { round }, returns json data
    - apiUnlockRound(round = 2) sends POST /api/bracket/unlock-round with { round }, returns json data
    - Socket.IO listeners for 'round2:locked' and 'round2:unlocked' update raceState.round2_status ('locked' | 'open') and raceState.round2_progress
    - useRace() exports apiLockRound and apiUnlockRound
  </behavior>
  <acceptance_criteria>
    - `apiLockRound` and `apiUnlockRound` functions exist in `RaceContext.jsx` and are included in `value` provider.
    - Sockets listen to `round2:locked` and `round2:unlocked`, updating local state accordingly.
  </acceptance_criteria>
  <action>
    In `client/src/context/RaceContext.jsx`:
    1. Define `apiLockRound`:
       ```javascript
       const apiLockRound = async (round = 2) => {
         const res = await fetch('/api/bracket/lock-round', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ round })
         });
         const data = await res.json();
         if (!data.success) throw new Error(data.error || 'Gagal mengunci babak');
         return data;
       };
       ```
    2. Define `apiUnlockRound`:
       ```javascript
       const apiUnlockRound = async (round = 2) => {
         const res = await fetch('/api/bracket/unlock-round', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ round })
         });
         const data = await res.json();
         if (!data.success) throw new Error(data.error || 'Gagal membuka kunci babak');
         return data;
       };
       ```
    3. In the WebSocket setup `useEffect`:
       Add listeners:
       ```javascript
       newSocket.on('round2:locked', (data) => {
         setRaceState(prev => ({
           ...prev,
           round2_status: 'locked',
           round2_progress: prev.round2_progress ? { ...prev.round2_progress, is_locked: true, can_finalize: false } : prev.round2_progress
         }));
       });

       newSocket.on('round2:unlocked', (data) => {
         setRaceState(prev => ({
           ...prev,
           round2_status: 'open',
           round2_progress: prev.round2_progress ? { ...prev.round2_progress, is_locked: false } : prev.round2_progress
         }));
       });
       ```
    4. Pass `apiLockRound` and `apiUnlockRound` into `RaceContext.Provider` value object.
  </action>
  <verify>
    <automated>node -e "import('./client/src/context/RaceContext.jsx').then(() => console.log('RaceContext syntax ok'));"</automated>
    <fails_when>Syntax error in RaceContext.jsx</fails_when>
  </verify>
  <done>RaceContext exports round lock API methods and listens to round lock socket events.</done>
</task>

<task type="auto">
  <name>Task 2: Create EliminationManager Component with 1-Click Winner, Round 2 Lock Workflow, and Filter Controls</name>
  <files>client/src/components/director/EliminationManager.jsx</files>
  <read_first>
    - client/src/screens/BracketDashboard.jsx
    - .planning/phases/19-race-director-elimination-command-center-ui/19-UI-SPEC.md
    - .planning/phases/19-race-director-elimination-command-center-ui/19-CONTEXT.md
  </read_first>
  <behavior>
    - Dynamic Round Selector tabs (Babak 2, Babak 3 / Perempat Final, Grand Final)
    - Babak 2 Progress Toolbar displaying total heats, completed count, pending count, and status badge (TERBUKA vs TERKUNCI)
    - "FINALISASI / KUNCI BABAK 2" button: disabled with remaining pending count when incomplete; illuminated with gold pulse when 100% completed
    - Clicking lock button opens Cyberpunk Confirmation Modal, calling apiLockRound on confirmation
    - "BUKA KUNCI BABAK 2" emergency unlock button appears when locked, opening confirmation modal calling apiUnlockRound
    - 3-Lane Heat Cards displaying Lane A (Pink), Lane B (Cyan), Lane C (Green) with racer name, team, and participant number
    - 1-Click "MENANG" button instantly advances winner without popup via apiAdvanceBracket(match.id, userId) per D-05
    - Completed heats visually highlighted with neon green border, SELESAI badge, and JUARA golden crown on winner per D-06
    - When Babak 2 is locked, MENANG buttons on Babak 2 are disabled/hidden to prevent accidental clicks
    - Instant omni-search for heat number, racer name, or team, plus status filters (Semua, Pending, Selesai)
  </behavior>
  <acceptance_criteria>
    - `client/src/components/director/EliminationManager.jsx` is created and exported.
    - Component renders round tabs, progress toolbar, search bar, status filters, and match grid.
    - Confirmation modals for lock and unlock operate cleanly with proper loading and error states.
    - 1-click winner selection functions smoothly.
  </acceptance_criteria>
  <action>
    Create `client/src/components/director/EliminationManager.jsx`:
    - Use `useRace()` to access `raceState`, `apiAdvanceBracket`, `apiLockRound`, `apiUnlockRound`.
    - Extract `matches = raceState.bracketMatches || []`.
    - Calculate available rounds and highest round (defaults to [2, 3]).
    - Round 2 Progress Calculation:
      Derive from `matches.filter(m => m.round_number === 2)`:
      `totalHeats = r2Matches.length`
      `completedHeats = r2Matches.filter(m => m.status === 'completed').length`
      `pendingHeats = totalHeats - completedHeats`
      `isLocked = raceState.round2_status === 'locked'`
      `canFinalize = totalHeats > 0 && pendingHeats === 0 && !isLocked`
    - Toolbar for Babak 2:
      When `selectedRound === 2`, render sticky progress banner:
      - Left: Badge `STATUS: TERBUKA (OPEN)` (cyan) or `STATUS: TERKUNCI (FINALIZED)` (amber/pink).
      - Center: Counter `Progres: {completedHeats} / {totalHeats} Heat Selesai` with visual progress bar.
      - Right:
        - If `!isLocked`:
          Render `<button>` with title `FINALISASI / KUNCI BABAK 2`:
          If `canFinalize`: neon amber background, pulsing glow, active cursor.
          If `!canFinalize`: disabled, opacity-40, text shows `KUNCI BABAK 2 (SISA ${pendingHeats} HEAT PENDING)`.
          Click opens `lockModalOpen`.
        - If `isLocked`:
          Render `<button>` `BUKA KUNCI BABAK 2 (EMERGENCY UNLOCK)` with red/amber styling.
          Click opens `unlockModalOpen`.
    - Modals:
      - Lock Modal: Title `FINALISASI & KUNCI BABAK 2`, explains that all heats are done and locking secures Round 3, buttons Batal & `KUNCI SEKARANG`.
      - Unlock Modal: Title `BUKA KUNCI BABAK 2 (DARURAT)`, explains reopening enables result revision, buttons Batal & `BUKA KUNCI`.
    - Match Cards:
      - Adapt layout from `BracketDashboard.jsx`: 3-lane horizontal row per heat.
      - Lane A Pink (`border-neonPink`, `bg-neonPink/20`), Lane B Cyan (`border-neonCyan`), Lane C Green (`border-neonGreen`).
      - In each lane:
        If contestant present and heat not completed:
          Show `MENANG` button. If `selectedRound === 2 && isLocked`, hide/disable `MENANG`.
        If contestant is winner:
          Display golden crown `JUARA` with pulsing green badge.
      - Card status header: `HEAT #{match.match_number}`, `SELESAI` (green check) or `PENDING` (amber clock).
    - Search & Filter bar:
      - Search input with clear button.
      - Status filter buttons: `SEMUA`, `PENDING`, `SELESAI`.
  </action>
  <verify>
    <automated>node -e "import('./client/src/components/director/EliminationManager.jsx').then(() => console.log('EliminationManager syntax ok'));"</automated>
    <fails_when>Syntax errors exist in EliminationManager.jsx</fails_when>
  </verify>
  <done>EliminationManager component provides full elimination command center interface.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor RaceDirectorDashboard.jsx to Host EliminationManager and Clean Obsolete Controls</name>
  <files>client/src/screens/RaceDirectorDashboard.jsx</files>
  <read_first>
    - client/src/screens/RaceDirectorDashboard.jsx
    - .planning/phases/19-race-director-elimination-command-center-ui/19-CONTEXT.md
    - .planning/phases/19-race-director-elimination-command-center-ui/19-UI-SPEC.md
  </read_first>
  <behavior>
    - Primary tab 'race' replaced with 'PUSAT KOMANDO ELIMINASI (BABAK 2 S/D FINAL)' hosting <EliminationManager />
    - Tab 2 remains 'MANAJEMEN BTO MANUAL (v3.0)' hosting <BtoManager />
    - Header preserves Event status, Kunci/Buka Kualifikasi Babak 1 button and modal
    - Obsolete digital controls removed: start race button, digital lock button, stopwatch inputs, countdown sound timer, re-race modal, admin override modal
  </behavior>
  <acceptance_criteria>
    - `RaceDirectorDashboard.jsx` imports and renders `<EliminationManager />`.
    - Obsolete digital racing controls and dead state are completely removed.
    - Tab switching between Elimination Command Center and BTO Manager works smoothly.
    - Header controls for qualifying lock/unlock remain functional.
  </acceptance_criteria>
  <action>
    In `client/src/screens/RaceDirectorDashboard.jsx`:
    1. Import `EliminationManager` from `../components/director/EliminationManager.jsx`.
    2. Remove obsolete state:
       - `finishTimes`
       - `reRaceModalOpen`, `selectedReRaceLanes`
       - `overrideLane`, `userList`, `searchUser`
       - API methods no longer used: `apiLockRace`, `apiStartRace`, `apiSubmitFinish`, `apiDeclareAllCO`, `apiDeclareReRace`, `apiAdminOverride`, `apiStartCountdown`, `apiResetCountdown`, `apiStopCountdown`.
    3. Update tab buttons:
       - Tab 1: `PUSAT KOMANDO ELIMINASI` (Icon: `GitBranch` or `LayoutDashboard`).
       - Tab 2: `MANAJEMEN BTO MANUAL (v3.0)` (Icon: `Trophy`).
    4. Render:
       - Header banner: PUSAT KOMANDO // RACE DIRECTOR COMMAND DESK, Status Kualifikasi Babak 1 (`BUKA KUALIFIKASI` / `KUNCI KUALIFIKASI`), and Lock/Unlock Qualifying Modals.
       - Tabs:
         `rdTab === 'race'` -> `<EliminationManager />`
         `rdTab === 'bto'` -> `<BtoManager />`
    5. Clean up dead JSX (old Lane Cards, Finish Input Form, Voice Countdown widget, Re-Race Modal, Admin Override Modal).
  </action>
  <verify>
    <automated>node -e "import('./client/src/screens/RaceDirectorDashboard.jsx').then(() => console.log('RaceDirectorDashboard syntax ok'));"</automated>
    <fails_when>Syntax error or broken import in RaceDirectorDashboard.jsx</fails_when>
  </verify>
  <done>RaceDirectorDashboard is refactored into a sleek, modern Elimination Command Center.</done>
</task>

<task type="auto">
  <name>Task 4: Verify Frontend Vite Build and Run Full Automated Test Suite</name>
  <files>client/dist, package.json</files>
  <read_first>
    - package.json
    - client/package.json
  </read_first>
  <behavior>
    - client/ builds successfully via npm run build
    - npm test passes 100% green with zero regressions
  </behavior>
  <acceptance_criteria>
    - `npm --prefix client run build` exits with code 0 and produces valid dist/ bundle.
    - `npm test` runs all backend and state test suites and exits with code 0.
  </acceptance_criteria>
  <action>
    1. Run Vite frontend build check:
       `$env:PATH = "C:\nvm4w\nodejs;" + $env:PATH; npm --prefix client run build`
       Ensure no JSX syntax, missing imports, or build warnings causing errors.
    2. Run full test suite:
       `$env:PATH = "C:\nvm4w\nodejs;" + $env:PATH; npm test`
       Ensure 100% green pass across all 14 test suites.
  </action>
  <verify>
    <automated>$env:PATH = "C:\nvm4w\nodejs;" + $env:PATH; npm --prefix client run build && npm test</automated>
    <fails_when>Vite build fails or any test in npm test fails</fails_when>
  </verify>
  <done>Frontend and backend are fully integrated, built, and verified.</done>
</task>

</tasks>
