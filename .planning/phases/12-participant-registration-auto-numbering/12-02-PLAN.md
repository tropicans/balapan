---
phase: "12"
plan: "02"
type: "execute"
wave: 2
depends_on: ["12-01"]
files_modified:
  - client/src/components/cashier/ParticipantNumberModal.jsx
  - client/src/components/cashier/ParticipantEditModal.jsx
  - client/src/components/cashier/ParticipantImportModal.jsx
  - client/src/components/cashier/ParticipantForm.jsx
  - client/src/components/cashier/ParticipantList.jsx
  - client/src/components/cashier/ParticipantRegistrationTab.jsx
  - client/src/screens/CashierDashboard.jsx
autonomous: true
requirements: [PARN-01, PARN-03, PARN-04, PARN-05, EVNT-03]
user_setup: []

must_haves:
  truths:
    - "Cashier Dashboard opens on 'Registrasi Peserta (v3.0)' as the default tab (D-10)"
    - "Cashier Header displays active event status, total participants, last issued number #N, Import CSV button, and Refresh button (D-11)"
    - "Submitting a registration shows a high-contrast modal displaying giant participant number '#42' (PARN-04, D-01, D-02)"
    - "Closing the number confirmation modal (via button, Enter, Space, or Esc) automatically returns focus to the racer name input field without requiring mouse interaction (D-03)"
    - "Cashier table provides single omni-search input with 200ms debounce searching by number or name/team (PARN-03, D-06)"
    - "Cashier table allows editing racer name or team typo while keeping participant number permanently locked (D-12)"
    - "Web UI allows importing CSV roster with preview modal showing valid and skipped counts before committing (PARN-05, D-07, D-08)"
    - "Vite client build completes cleanly without syntax or bundling errors"
  artifacts:
    - path: "client/src/components/cashier/ParticipantNumberModal.jsx"
      provides: "High-contrast confirmation modal displaying giant '#42' with keyboard dismiss & focus return"
    - path: "client/src/components/cashier/ParticipantEditModal.jsx"
      provides: "Modal for editing participant name & team without mutating participant_number"
    - path: "client/src/components/cashier/ParticipantImportModal.jsx"
      provides: "CSV file upload & preview modal with valid/skipped rows summary before execution"
    - path: "client/src/components/cashier/ParticipantForm.jsx"
      provides: "Participant registration form with auto-focus ref and Enter key submission"
    - path: "client/src/components/cashier/ParticipantList.jsx"
      provides: "Participant table with omni-search and edit actions"
    - path: "client/src/components/cashier/ParticipantRegistrationTab.jsx"
      provides: "v3.0 tab combining form, list, and modals with realtime WebSocket syncing"
    - path: "client/src/screens/CashierDashboard.jsx"
      provides: "Updated Cashier Dashboard with Tab 1 as default and v3.0 telemetry header"
  key_links:
    - from: "client/src/components/cashier/ParticipantForm.jsx"
      to: "client/src/components/cashier/ParticipantNumberModal.jsx"
      via: "Opens modal on successful registration and handles focus return on close"
      pattern: "ParticipantNumberModal"
    - from: "client/src/components/cashier/ParticipantRegistrationTab.jsx"
      to: "/api/participants"
      via: "Fetches participant list and handles realtime WebSocket events"
      pattern: "/api/participants"
    - from: "client/src/screens/CashierDashboard.jsx"
      to: "client/src/components/cashier/ParticipantRegistrationTab.jsx"
      via: "Mounted as Tab 1 (activeTab === 'v3_registration')"
      pattern: "ParticipantRegistrationTab"
---

<objective>
Implement the Cashier UI sub-components, high-contrast giant confirmation modal (#42), omni-search table, CSV roster import modal, and integrate them into `CashierDashboard.jsx` as the primary default tab with comprehensive header telemetry.

Purpose: Provide cashiers with an ultra-fast, keyboard-driven registration workflow allowing seamless participant check-in, coupon writing, quick search, typo fixing, and bulk CSV ingestion without touching the mouse.
Output: Sub-components in `client/src/components/cashier/`, updated `CashierDashboard.jsx`, and a verified client build.
Citations: Fulfills PARN-01, PARN-03, PARN-04, PARN-05, EVNT-03 and implements D-01, D-02, D-03, D-06, D-07, D-08, D-10, D-11, D-12.
</objective>

<execution_context>
@C:/Users/yudhiar/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/yudhiar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-participant-registration-auto-numbering/12-CONTEXT.md
@.planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md
@.planning/phases/12-participant-registration-auto-numbering/12-VALIDATION.md
@client/src/screens/CashierDashboard.jsx
@client/src/components/ui/CyberCard.jsx
@client/src/components/ui/CyberButton.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Cashier Modal Sub-components (Giant Number Modal, Edit Modal, CSV Import Modal)</name>
  <files>client/src/components/cashier/ParticipantNumberModal.jsx, client/src/components/cashier/ParticipantEditModal.jsx, client/src/components/cashier/ParticipantImportModal.jsx</files>
  <read_first>
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 7.3 "Modal Konfirmasi Nomor Raksasa (D-01, D-02, D-03)"
    - client/src/components/cashier/VoidPackageModal.jsx
    - client/src/components/ui/CyberButton.jsx
  </read_first>
  <behavior>
    - ParticipantNumberModal renders giant '#42' (7xl/8xl font-orbitron) in electric cyan with glow effect per D-01, D-02
    - ParticipantNumberModal shows racer name and team ('-' if null) and writing reminder note
    - ParticipantNumberModal traps Enter/Escape/Space keys to invoke onClose callback per D-01, D-03
    - ParticipantEditModal renders participant number as locked badge, allows editing name (required) and team (optional) per D-12
    - ParticipantEditModal calls PUT /api/participants/:id and calls onSuccess on completion
    - ParticipantImportModal allows selecting or pasting CSV, calls POST /api/participants/import-preview to show valid and skipped rows with reasons per D-07, D-08
    - ParticipantImportModal executes POST /api/participants/import on confirmation, shows result and notifies parent per D-09
  </behavior>
  <acceptance_criteria>
    - `client/src/components/cashier/ParticipantNumberModal.jsx` displays `#{participant.participant_number}` in 7xl/8xl text and closes on Enter/Esc.
    - `client/src/components/cashier/ParticipantEditModal.jsx` locks participant number and updates name and team.
    - `client/src/components/cashier/ParticipantImportModal.jsx` previews and executes CSV imports.
  </acceptance_criteria>
  <action>
    Create `client/src/components/cashier/ParticipantNumberModal.jsx` per RESEARCH § 7.3:
    - High-contrast modal overlay (`bg-black/85 backdrop-blur-md`).
    - Obsidian card with border-2 border-neonCyan and shadow `0 0 50px rgba(0,240,255,0.3)`.
    - Giant number display: font-orbitron font-black text-7xl md:text-8xl text-neonCyan drop-shadow-[0_0_20px_rgba(0,240,255,0.6)] showing `#{participant.participant_number}` per D-01, D-02.
    - Racer name uppercase and team tag: `TIM: {participant.team_name || '-'}` per D-05.
    - Physical instruction: "Tulis nomor ini pada lembar kupon fisik peserta."
    - Button: "SELESAI / LANJUT DAFTAR (ENTER)" with ref to auto-focus on mount.
    - Keyboard listener: `window.addEventListener('keydown')` listening for `Enter`, `Escape`, `' '` calling `onClose()` per D-03.

    Create `client/src/components/cashier/ParticipantEditModal.jsx` per D-12:
    - Controlled form with `name` and `teamName`.
    - Read-only locked badge displaying `Nomor Peserta: #{participant.participant_number}` with a lock icon.
    - Input `name` (required) and `teamName` (optional).
    - Save action calling `PUT /api/participants/${participant.id}`.
    - Handle error state and loading state.
    - Batal and Simpan buttons.

    Create `client/src/components/cashier/ParticipantImportModal.jsx` per D-07, D-08, D-09:
    - File input (`.csv`) with drag-and-drop support and/or textarea paste.
    - On file/text selection, immediately call `POST /api/participants/import-preview`.
    - Display preview summary:
      - Valid rows count (badge green) and preview list (first 5-10 rows).
      - Skipped rows count (badge amber/red) and table of skipped rows with row number and rejection reason.
    - Execute import button: "IMPORT {preview.valid.length} PESERTA" calling `POST /api/participants/import`.
    - On success: play sound, notify parent callback `onSuccess()`, and close modal.
  </action>
  <verify>
    <automated>npm --prefix client run build</automated>
    <fails_when>Vite build fails on syntax errors or missing imports in modal sub-components</fails_when>
  </verify>
  <done>ParticipantNumberModal, ParticipantEditModal, and ParticipantImportModal are created and verified by Vite build.</done>
</task>

<task type="auto">
  <name>Task 2: Build Participant Form, Omni-Search Table, Registration Tab, and Integrate into Cashier Dashboard</name>
  <files>client/src/components/cashier/ParticipantForm.jsx, client/src/components/cashier/ParticipantList.jsx, client/src/components/cashier/ParticipantRegistrationTab.jsx, client/src/screens/CashierDashboard.jsx</files>
  <read_first>
    - client/src/screens/CashierDashboard.jsx
    - client/src/components/cashier/CouponRegistrationForm.jsx
    - client/src/components/cashier/CouponPackageList.jsx
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 3 "Architecture & Integration Points"
  </read_first>
  <behavior>
    - ParticipantForm has racer name input (required) and team name input (optional)
    - ParticipantForm autofocuses racer name input on mount and re-focuses on modal close per D-03
    - ParticipantForm submits on Enter or click, calling POST /api/participants, triggering audio and opening ParticipantNumberModal
    - ParticipantList renders participant rows with '#N', racer name, team ('-' if empty), registration timestamp, and 'Edit' button per D-12
    - ParticipantList provides omni-search input with 200ms debounce that queries by number or text per D-06
    - ParticipantRegistrationTab unites ParticipantForm and ParticipantList in a 2-column layout (40% / 60%)
    - ParticipantRegistrationTab listens to WebSocket events 'participant_registered', 'participant_updated', 'participants_imported', 'STATE_UPDATE' to auto-refresh table
    - CashierDashboard sets 'v3_registration' as default active tab per D-10
    - CashierDashboard Header shows Active Event name + badge, total participants, latest number #N, Import CSV button, and Refresh button per D-11
  </behavior>
  <acceptance_criteria>
    - `CashierDashboard.jsx` defaults to tab `'v3_registration'`.
    - Cashier Header displays active event status, total count, latest number `#N`, Import CSV button, and Refresh button.
    - Keyboard-only flow: Enter registers -> Number modal opens -> Enter/Esc closes modal -> Name input receives focus.
    - Table omni-search filters correctly with 200ms debounce.
    - `npm --prefix client run build` and `npm test` exit code 0.
  </acceptance_criteria>
  <action>
    Create `client/src/components/cashier/ParticipantForm.jsx`:
    - Provide `nameInputRef` and state for `name` and `teamName`.
    - Auto-focus `nameInputRef` on component mount (`useEffect(() => { nameInputRef.current?.focus(); }, [])`).
    - Provide `focusNameInput()` helper so parent or modal close handler can focus `nameInputRef.current?.focus()` after modal dismiss per D-03.
    - Form submission calls `POST /api/participants` with `{ name, team_name: teamName || null }`.
    - On success: play tone `sound.playTone(880, 'sine', 0.08, 0.1)`, clear fields, and invoke `onSuccess(createdParticipant)` which displays `ParticipantNumberModal`.
    - If error occurs (e.g. no active event), display clear cyberpunk alert message.

    Create `client/src/components/cashier/ParticipantList.jsx`:
    - Omni-search bar at top with icon, clear button, and placeholder "Cari #nomor atau nama/tim..." per D-06.
    - Implement 200ms debounce for search query before triggering API fetch.
    - Render participant table:
      - Column `#`: formatted as `#{p.participant_number}` in font-mono font-bold text-neonCyan (D-02).
      - Column Nama: uppercase bold text.
      - Column Tim: `p.team_name || '-'` in neonPink/cyberSilver font-mono (D-05).
      - Column Waktu: formatted local time (HH:mm:ss).
      - Column Aksi: "Edit" button opening `ParticipantEditModal` (D-12).
    - Handle empty state, loading state, and refresh button.

    Create `client/src/components/cashier/ParticipantRegistrationTab.jsx`:
    - Responsive 2-column split layout (Left 5 cols: `ParticipantForm`, Right 7 cols: `ParticipantList`).
    - Manage state for `registeredModalParticipant`, `editModalParticipant`, `importModalOpen`.
    - When `ParticipantNumberModal` calls `onClose`, invoke `formRef.current?.focusNameInput()` with a 50ms timeout to ensure the DOM has re-rendered before focusing (per D-03).
    - Register socket listeners for `participant_registered`, `participant_updated`, `participants_imported`, and `STATE_UPDATE` to automatically reload participants.

    Update `client/src/screens/CashierDashboard.jsx`:
    - Set default `activeTab` state to `'v3_registration'` per D-10.
    - Retain existing tabs `'physical'` ("Paket Kupon Fisik (Pre-Printed)") and `'digital'` ("Top Up Saldo Digital") for backward compatibility during transition.
    - Update Header Banner per D-11:
      - Display Active Event: `{activeEvent ? activeEvent.nama : 'TIDAK ADA EVENT AKTIF'}` with a glowing badge `[ACTIVE]` (or red badge `[NO EVENT]`).
      - Telemetry pills: "TOTAL PESERTA: {totalParticipants}" and "NOMOR TERAKHIR: #{latestNumber}".
      - Header actions: Add "IMPORT CSV" button (opens `ParticipantImportModal`) and "REFRESH" button.
    - Add tab button 1: "Registrasi Peserta (v3.0)" as first tab, styled with neonCyan active glow.
    - Render `ParticipantRegistrationTab` when `activeTab === 'v3_registration'`.
  </action>
  <verify>
    <automated>npm --prefix client run build</automated>
    <fails_when>Vite build fails or client fails to compile CashierDashboard and participant sub-components</fails_when>
  </verify>
  <done>Cashier Dashboard is fully upgraded to v3.0 with auto-numbering, high-contrast modal, omni-search, and CSV import.</done>
</task>

</tasks>
