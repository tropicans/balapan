---
phase: "13"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - server/services/btoService.js
  - server/raceManager.js
  - server/index.js
  - server/tests/bto-service.test.js
  - package.json
autonomous: true
requirements: [BTO-01, BTO-02, BTO-03, BTO-04, EVNT-04]
user_setup: []

must_haves:
  truths:
    - "Panitia can record a manual lap time for a participant via participant_number or user_id (BTO-01)"
    - "System only updates existing BTO if new_time is faster than existing personal best (BTO-02)"
    - "getLeaderboard returns top-N fastest times scoped to active event (BTO-03, EVNT-04)"
    - "System detects when a new time beats the overall #1 event best and emits NEW_BTO_RECORD (BTO-04)"
    - "RaceManager.getFullState() returns btoLeaderboard sourced from bto_records"
    - "REST endpoints /api/bto, /api/bto/leaderboard, /api/bto/record are active and protected"
    - "Automated test suite server/tests/bto-service.test.js passes all cases and is wired into package.json"
artifacts:
  - path: "server/services/btoService.js"
    provides: "Domain service: recordBtoTime, getBtoLeaderboard, getParticipantBto, deleteBtoRecord"
  - path: "server/tests/bto-service.test.js"
    provides: "Unit and integration tests for BTO domain service and endpoints"
---

<objective>
Implement the manual BTO domain service, event-scoped storage in `bto_records`, personal-best replacement logic, top-1 record detection, REST API endpoints, and test suite.
</objective>
