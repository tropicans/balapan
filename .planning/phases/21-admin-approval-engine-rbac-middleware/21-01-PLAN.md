---
phase: "21"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - server/services/authService.js
  - server/middleware/authMiddleware.js
  - server/index.js
  - server/tests/admin-approval-rbac.test.js
  - package.json
autonomous: true
requirements: [APPR-03, APPR-04, APPR-05, APPR-06]
user_setup: []

must_haves:
  truths:
    - "Admin can list pending, approved, and suspended users via GET /api/admin/users (D-01, APPR-03)"
    - "Admin can approve pending users with specific role via POST /api/admin/users/:id/approve (D-02, APPR-04)"
    - "Admin can update roles and suspend users with active session cleanup, while super_admin is protected (D-03, APPR-05)"
    - "RBAC middleware (requireAuth, requireApproved, requireRole) guards endpoints and rejects unauthorized requests (D-04, APPR-06)"
    - "Approval actions broadcast WebSocket events for real-time synchronization (D-05)"
    - "Automated test suite server/tests/admin-approval-rbac.test.js passes 100% green"
  artifacts:
    - path: "server/services/authService.js"
      provides: "Admin user management queries (listAppUsers, approveAppUser, updateAppUserRole, setAppUserStatus)"
    - path: "server/middleware/authMiddleware.js"
      provides: "RBAC middleware: requireAuth, requireApproved, requireRole"
    - path: "server/index.js"
      provides: "Admin user management REST API endpoints and protected operational route guards"
    - path: "server/tests/admin-approval-rbac.test.js"
      provides: "Automated test suite for user approval, role assignment, revocation, and RBAC middleware"
---

<objective>
Implement the Admin Approval Engine and Role-Based Access Control (RBAC) middleware.
Enables `tropicans@gmail.com` to inspect pending Google sign-ins, approve users with roles (cashier, race_director, scrutineer, admin, viewer), suspend accounts, and protect operational routes.
</objective>

<tasks>

<task type="auto" id="21-01-01">
<name>Extend authService.js with User Management Domain Methods</name>
<description>
Implement `listAppUsers(filter)`, `approveAppUser(userId, role, approvedBy)`, `updateAppUserRole(userId, role, requestedBy)`, and `setAppUserStatus(userId, status, requestedBy)` with super admin protection.
</description>
</task>

<task type="auto" id="21-01-02">
<name>Create authMiddleware.js with requireAuth, requireApproved, requireRole</name>
<description>
Implement reusable Express middleware for authentication and authorization.
</description>
</task>

<task type="auto" id="21-01-03">
<name>Mount Admin Endpoints and WebSocket Broadcast in server/index.js</name>
<description>
Mount `GET /api/admin/users`, `POST /api/admin/users/:id/approve`, `POST /api/admin/users/:id/role`, `POST /api/admin/users/:id/status`. Emit `user:updated` WebSocket events.
</description>
</task>

<task type="auto" id="21-01-04">
<name>Create server/tests/admin-approval-rbac.test.js and wire into npm test</name>
<description>
Write comprehensive test suite for user approval, role changes, super admin immunity, and middleware rejection. Verify 100% green.
</description>
</task>

</tasks>
