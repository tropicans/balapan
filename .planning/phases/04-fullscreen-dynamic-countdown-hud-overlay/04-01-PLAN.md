---
phase: 04-fullscreen-dynamic-countdown-hud-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/index.css
  - client/src/screens/RealtimeTV.jsx
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "Realtime TV HUD renders an eye-catching full-viewport modal overlay whenever countdown.active is true"
    - "During active countdown (seconds 10 down to 1), a giant pulsing countdown digit in font-orbitron is centered with high-contrast scanlines, glowing radial backdrop, and Indonesian voice text prompt"
    - "When countdown completes or is stopped early, the overlay renders explosive GO / LEPAS MOBIL or READY banners with emerald neon flash and auto-dismiss"
    - "Realtime TV maintains 16:9 circuit broadcast readability without UI clipping or overflow"
    - "Client build passes cleanly with zero errors"
  artifacts:
    - client/src/screens/RealtimeTV.jsx
    - client/src/index.css
---

<objective>
Implement Phase 4: Fullscreen Dynamic Countdown HUD Overlay on Circuit TV (UI-03)
1. Add high-contrast cyberpunk HUD animations in index.css (countdown pulse, radial neon glow, scanline sweeps).
2. Integrate full-viewport Countdown HUD Overlay in RealtimeTV.jsx subscribing to `countdown` from RaceContext (connected to socket events `COUNTDOWN_STARTED`, `COUNTDOWN_TICK`, `COUNTDOWN_COMPLETE`, and `COUNTDOWN_STOPPED`).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add Countdown Overlay Animations and Backdrop Styles in index.css</name>
  <files>client/src/index.css</files>
  <action>
    Add CSS styles and animations for the circuit TV countdown HUD:
    - `.countdown-radial-bg`: Deep dark backdrop with radial crimson/pink glow during ticking, shifting to neon green glow on complete.
    - `.countdown-number-pulse`: Scale keyframe animation for each tick (10 down to 1) providing dramatic visual impact for viewers across the venue.
    - `.text-glow-countdown`: Intensive high-contrast text shadow for the giant countdown numerals.
    - `.countdown-scanlines`: Subtle CRT horizontal scanline effect across the countdown container.
  </action>
  <verify>Check client/src/index.css is valid CSS and compiles</verify>
  <done>CSS animations and utilities for Countdown HUD ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dynamic Countdown HUD Overlay in RealtimeTV.jsx</name>
  <files>client/src/screens/RealtimeTV.jsx</files>
  <action>
    In client/src/screens/RealtimeTV.jsx:
    - Destructure `countdown` from `useRace()` hook: `{ active, seconds, word, status, message }`.
    - When `countdown.active` is true, render a fixed full-screen overlay (`fixed inset-0 z-50 flex items-center justify-center`):
      - Header indicator: "PERSIAPAN PELEPASAN MOBIL // BABAK 2 ELIMINASI" with telemetry metadata.
      - Body stage:
        - When `status === 'running'`:
          - Giant numeral (text-8xl md:text-[14rem] font-black font-orbitron text-neonPink text-glow-countdown countdown-number-pulse).
          - Indonesian spelled word badge below numeral (`SEPULUH`, `SEMBILAN`, etc.) in glowing amber/gold.
          - 10-segment circular/horizontal progress bar reflecting remaining seconds.
        - When `status === 'complete'`:
          - Explosive green neon screen state (`bg-neonGreen/10 border-neonGreen`).
          - Massive headline "GO! LEPAS MOBIL!" with glowing chevrons.
          - Sub-banner "MARSHAL: LEPAS KETIGA MOBIL SEKARANG!".
        - When `status === 'stopped'`:
          - Cyan / Emerald "READY - SIAP LEPAS!" banner indicating RD stopped early.
    - Ensure zero layout shift on the background TV HUD when overlay triggers and auto-hides.
  </action>
  <verify>Run npm --prefix client run build to verify syntax and bundling</verify>
  <done>RealtimeTV displays high-impact fullscreen dynamic countdown HUD</done>
</task>

<task type="auto">
  <name>Task 3: Production Build and Verification</name>
  <files>client/src/screens/RealtimeTV.jsx, client/src/index.css</files>
  <action>
    Run client Vite build and verify that all JSX and CSS bundle properly without errors.
  </action>
  <verify>npm --prefix client run build succeeds</verify>
  <done>Phase 4 implementation verified with zero build regressions</done>
</task>
</tasks>
