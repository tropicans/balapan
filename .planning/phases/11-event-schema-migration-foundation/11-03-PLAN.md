---
phase: 11-event-schema-migration-foundation
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - client/src/screens/EventManagementDashboard.jsx
  - client/src/components/ui/Navbar.jsx
  - client/src/App.jsx
  - server/tests/migration-foundation.test.js
autonomous: false
requirements: [EVNT-01, EVNT-02]
user_setup: []

must_haves:
  truths:
    - "Panitia can open a 'Manajemen Event' tab and see active + archived events with their nama and tanggal"
    - "Panitia can create an event from the screen (nama, tanggal, jumlah lap, catatan) and it appears in the list"
    - "Panitia can activate or archive an event from the screen; the active event is visually distinguished"
    - "The screen reflects changes pushed over the existing STATE_UPDATE socket event without a reload"
    - "The client production build succeeds with the new screen wired into the router and navbar"
    - "A v2.0-shaped database still boots: schema_version records version 1, the new columns exist, and no table was dropped"
    - "Implements decisions D-12 (event fields in UI), D-13 (default Event 1 shown active), D-14 (archive-only action), D-15 (new Manajemen Event screen)"
  artifacts:
    - path: "client/src/screens/EventManagementDashboard.jsx"
      provides: "Manajemen Event screen: list + create form + activate/archive actions"
    - path: "client/src/components/ui/Navbar.jsx"
      provides: "Nav entry { id: 'events', label: 'Manajemen Event', path: '/events' }"
    - path: "client/src/App.jsx"
      provides: "Route branch `activeScreen === 'events'`"
  key_links:
    - from: "client/src/screens/EventManagementDashboard.jsx"
      to: "/api/events"
      via: "fetch('/api/events') and POST activate/archive"
      pattern: "fetch\\('/api/events"
    - from: "client/src/App.jsx"
      to: "client/src/screens/EventManagementDashboard.jsx"
      via: "renders <EventManagementDashboard /> when activeScreen === 'events'"
      pattern: "EventManagementDashboard"
    - from: "client/src/components/ui/Navbar.jsx"
      to: "/events route"
      via: "screens[] entry with path '/events'"
      pattern: "'/events'"
---

<objective>
Ship the Manajemen Event screen (D-15) wired into the navbar and router, and add the v2.0 boot-safety regression test that proves the phase's success criterion 5.

Purpose: EVNT-01/EVNT-02 are only complete when panitia can actually use the flow; the boot test proves the migration is non-destructive against an existing v2.0 database.
Output: `EventManagementDashboard.jsx`, navbar + router wiring, a client build, and an appended v2.0-shaped boot regression test.
</objective>

<execution_context>
@C:/Users/yudhiar/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/yudhiar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-event-schema-migration-foundation/11-CONTEXT.md
@.planning/phases/11-event-schema-migration-foundation/11-RESEARCH.md
@.planning/phases/11-event-schema-migration-foundation/11-PATTERNS.md
@.planning/phases/11-event-schema-migration-foundation/11-VALIDATION.md
@client/src/screens/CashierDashboard.jsx
@client/src/components/ui/Navbar.jsx
@client/src/App.jsx
@.planning/phases/11-event-schema-migration-foundation/11-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manajemen Event screen + navbar + router wiring</name>
  <files>client/src/screens/EventManagementDashboard.jsx, client/src/components/ui/Navbar.jsx, client/src/App.jsx</files>
  <read_first>
    - client/src/screens/CashierDashboard.jsx lines 1-24 (imports + component shape) and 51-98 (fetch + envelope + socket sync + cleanup)
    - client/src/components/ui/Navbar.jsx lines 4-17 (lucide imports) and 44-53 (`screens[]` array)
    - client/src/App.jsx lines 8-15 (imports), 18-32 (route detection), 41-48 (render branches)
    - .planning/phases/11-event-schema-migration-foundation/11-PATTERNS.md §"client/src/screens/EventManagementDashboard.jsx (new)" and §"client/src/components/ui/Navbar.jsx (modify)" and §"client/src/App.jsx (modify)"
  </read_first>
  <acceptance_criteria>
    - `EventManagementDashboard.jsx` exports `EventManagementDashboard`, uses `useState`/`useEffect`/`useCallback`, `CyberButton`, `CyberCard`, `clsx`, and reads `GET /api/events` with the `{ success, data }` envelope
    - The screen offers a create form (nama required, tanggal, jumlah lap, catatan) that POSTs `/api/events`, and per-row activate (`POST /api/events/:id/activate`) and archive (`POST /api/events/:id/archive`) actions
    - The active event is visually distinguished; archived events remain listed and cannot be deleted from the UI
    - The screen subscribes `socket.on('STATE_UPDATE', refetch)` via `useRace()` and removes the listener with `socket.off` on unmount
    - Error states render Indonesian messages (`'Gagal memuat daftar event'`, `'Koneksi ke server database turnamen terputus'`)
    - `Navbar.jsx` `screens[]` gains `{ id: 'events', label: 'Manajemen Event', path: '/events' }` with an imported lucide icon; no dark link
    - `App.jsx` route detection returns `'events'` for `/events` or `#events` and renders `<EventManagementDashboard />`
    - `npm --prefix client run build` exits 0
  </acceptance_criteria>
  <action>
Create `client/src/screens/EventManagementDashboard.jsx` modelled on `CashierDashboard.jsx`: local state for events, loading, error, and form fields; a `fetchEvents` callback hitting `/api/events` and handling `data.success`; a `useEffect` that fetches on mount; a socket effect subscribing `STATE_UPDATE` when `socket` from `useRace()` is available, with `socket.off` cleanup. Render a create form (nama, tanggal, jumlah lap default 3, catatan) that POSTs `/api/events` and refetches, a list of events showing nama/tanggal/jumlah_lap/catatan with a badge for `status='active'`, and activate/archive buttons per row that POST to the corresponding endpoint and refetch. Show Indonesian error banners on failed fetches, and confirm the create/enable flow with a success feedback line. Keep styling consistent with the cyberpunk utility classes used elsewhere (`bg-obsidian`, `clip-cyber`, `font-orbitron`, `neonCyan` accent).
Add a navbar entry in `client/src/components/ui/Navbar.jsx` `screens[]` with `id: 'events'`, `label: 'Manajemen Event'`, `path: '/events'` and import a suitable lucide icon (e.g. `Calendar`) in the existing import block.
Wire the route in `client/src/App.jsx`: import the screen, extend the `activeScreen` initializer with a `/events` / `#events` branch, and add `{activeScreen === 'events' && <EventManagementDashboard />}` to the render area.
  </action>
  <verify>
    <automated>npm --prefix client run build</automated>
  </verify>
  <done>Manajemen Event screen is reachable at /events, lists events, and creates/activates/archives them through the API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: v2.0 boot-safety regression against an existing database file</name>
  <files>server/tests/migration-foundation.test.js</files>
  <read_first>
    - server/tests/race-flow.test.js lines 1-16 and 193-202 (harness + teardown)
    - server/db.js `initDatabase()` (lines 120-275) — legacy tables, migration call, seed gate
    - .planning/phases/11-event-schema-migration-foundation/11-RESEARCH.md §"Common Pitfalls" 8 and §"Validation Architecture"
    - .planning/phases/11-event-schema-migration-foundation/11-VALIDATION.md §"Per-Task Verification Map" row 11-03-04
  </read_first>
  <behavior>
    - Test: boot `initDatabase()` twice against the same temp `DB_PATH`; the second boot applies 0 migration steps and does not throw (idempotent re-open of the file written by the first boot)
    - Test: after boot, `schema_version` has version 1, `PRAGMA table_info(users)` includes `participant_number`, and `sqlite_master` still lists `coupons` and `races` (no table dropped in this phase)
    - Test: `RaceManager.getFullState()` returns without throwing and contains `activeRace` and `bracketMatches`
    - Test: every `role='participant'` row has non-NULL `event_id` and `participant_number`, and `SELECT COUNT(*) FROM users WHERE participant_number IS NOT NULL AND event_id IS NULL` is 0
    - Test: migrating does not leave the temp DB in a partial state — a second `runMigrations(db)` call reports `{ applied: 0 }`
  </behavior>
  <acceptance_criteria>
    - The new assertions live in `server/tests/migration-foundation.test.js` and use a fresh temp `DB_PATH` distinct from the earlier tasks' file
    - No assertion is skipped on Windows or Linux; path handling uses `path.join` + `fileURLToPath`
    - The test cleans up its temp DB in a `finally` block
    - `node server/tests/migration-foundation.test.js` exits 0
    - `npm test` exits 0 (full suite including the appended migration test)
  </acceptance_criteria>
  <action>
Append a v2.0 boot-safety section to `server/tests/migration-foundation.test.js`. Use a second unique temp DB path so the first-boot assertions are independent. Run `initDatabase()` once to create the file, then re-open it by calling `initDatabase()` again (or `runMigrations(db)` directly) and assert it reports zero applied steps and does not throw — this is the idempotency proof that models re-opening a deployed v2.0 database. Assert the survival of the legacy `coupons` and `races` tables in `sqlite_master` (success criterion 5: no drop in this phase), the presence of `users.participant_number` via `PRAGMA table_info`, `schema_version` version 1, and that `RaceManager.getFullState()` returns `activeRace` plus `bracketMatches` without throwing. Assert the numbered-row invariant `participant_number IS NOT NULL AND event_id IS NULL` is empty. Clean up all temp DB files created by the test in a `finally` block.
  </action>
  <verify>
    <automated>node server/tests/migration-foundation.test.js && npm test</automated>
  </verify>
  <done>Boot against an existing database file is idempotent, non-destructive, and leaves a readable legacy state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Manajemen Event screen at `/events` (create / activate / archive) backed by `/api/events`, plus the idempotent v2.0 boot-safety test.</what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` (or `npm start` after `npm run build`).
    2. Open the Manajemen Event tab (route `/events`). Confirm the boot-created `Event 1` is listed and marked active.
    3. Create a new event "Turnamen Uji <today>" with a tanggal and jumlah lap 3. Confirm it appears in the list.
    4. Activate the new event. Confirm it becomes active and `Event 1` shows as archived, and that both rows are still listed (nothing deleted).
    5. Archive the new event. Confirm it stays in the list with archived status.
    6. Open a second browser tab on the same screen and replay steps 2-5. Confirm the other tab updates without a manual reload (STATE_UPDATE).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form input → `/api/events` | Untrusted nama/tanggal/catatan/jumlah lap cross here; length limits are enforced server-side |
| server → browser render | Event fields created by panitia are rendered by React |
| existing v2.0 DB file → migration | The phase must read and upgrade without destroying data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `EventManagementDashboard.jsx` form submissions | mitigate | Screen only sends field values as a JSON body; the server (plan 11-02) binds all values with `db.prepare`. The client never builds SQL |
| T-11-05 | Denial of Service | oversized nama/catatan from the form | mitigate | Form uses `maxLength` on nama/catatan inputs; server-side limits remain the enforcing control |
| T-11-06 | Information Disclosure | event list rendering | mitigate | `GET /api/events` returns only event metadata; no participant or cross-event data is rendered on this screen |
| T-11-04 | Tampering / Elevation | destructive migration during boot | transfer | Gate implemented in plan 11-01; this plan adds the regression test proving no legacy table is dropped |
| T-11-02 | Tampering | backup path | transfer | Handled in plan 11-01 |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed by this plan (zero new runtime deps; client build uses the existing tree) |
</threat_model>

<verification>
- `npm --prefix client run build` green
- `node server/tests/migration-foundation.test.js` green
- `npm test` green
- Manual: Manajemen Event create/activate/archive works and syncs across tabs
- Manual: `data/backups/tamiya-<ts>.sqlite` exists after a migration runs against an existing DB file
</verification>

<success_criteria>
- Manajemen Event screen reachable at `/events` with create/activate/archive and realtime refresh
- v2.0-shaped DB still boots; `schema_version` = 1; no legacy table dropped
- Phase 11 success criteria 1-5 all satisfied
</success_criteria>

<output>
Create `.planning/phases/11-event-schema-migration-foundation/11-03-SUMMARY.md` when done
</output>

## Decisions Covered

- **D-12** — Manajemen Event screen creates/edits `nama`, `tanggal`, `catatan`, `jumlah_lap`.
- **D-13** — screen shows the auto-created default `Event 1` as active on first boot.
- **D-14** — archive-only action (no delete) surfaced in the UI.
- **D-15** — new **Manajemen Event** screen delivered.

## Artifacts this phase produces

- Files: `client/src/screens/EventManagementDashboard.jsx`
- Modified: `client/src/components/ui/Navbar.jsx`, `client/src/App.jsx`, `server/tests/migration-foundation.test.js`
- Route: `/events` (client nav + router branch)
- Screen: Manajemen Event (create / activate / archive)