---
phase: 05-3-lane-elimination-backend-engine-auto-advance-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db.js
  - server/raceManager.js
  - server/tests/bracket-3lane.test.js
autonomous: true
requirements:
  - ELIM-01
  - ELIM-02
  - ELIM-03
must_haves:
  truths:
    - "Setiap heat pertandingan eliminasi mendukung 3 kontestan (user_id_1, user_id_2, user_id_3 memetakan ke Jalur A, Jalur B, Jalur C)"
    - "Peserta yang lolos Scrutineer Babak 1 otomatis mengisi slot Jalur A -> B -> C di Babak 2 secara berurutan dan tanpa duplikasi"
    - "Sistem otomatis membuat heat baru di Babak 2 saat seluruh slot heat yang ada telah penuh, mendukung hingga 100+ heat"
    - "Memilih pemenang di Heat Babak R otomatis mempromosikan pemenang ke slot kosong di Babak R+1 dengan pola reduksi 3:1 hingga mencapai Grand Final"
    - "Seluruh test suite (server/tests/bracket-3lane.test.js dan server/tests/race-flow.test.js) lulus 100% tanpa error atau regresi"
  artifacts:
    - server/db.js
    - server/raceManager.js
    - server/tests/bracket-3lane.test.js
---

<objective>
Mengembangkan backend engine sistem eliminasi 3-jalur (Jalur A, B, C) dengan kapasitas pembuatan heat dinamis hingga 100+ heat dan auto-advance pemenang antar-ronde secara berjenjang (3:1 tree reduction) hingga Grand Final.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold Wave 0 Test Suite (bracket-3lane.test.js)</name>
  <files>server/tests/bracket-3lane.test.js</files>
  <action>
    Buat unit test suite komprehensif `server/tests/bracket-3lane.test.js` yang menguji:
    1. Seeding 3 pembalap ke dalam 1 heat (Line A Pink, Line B Cyan, Line C Green) -> verifikasi `user_id_1`, `user_id_2`, `user_id_3` terisi dengan benar (ELIM-01).
    2. Pencegahan duplikasi seeding: pembalap yang sudah ada di Babak 2 tidak di-insert ulang.
    3. Skalabilitas alokasi heat: simulasi seeding 30 kontestan menghasilkan 10 heat Babak 2 terisi penuh; simulasi hingga 300 kontestan (100 heat) teralokasi secara rapi tanpa error (ELIM-02).
    4. Auto-advance 3:1: 3 pemenang dari Heat 1, Heat 2, Heat 3 Babak 2 secara otomatis mengisi Jalur A, B, C pada Babak 3 (ELIM-03).
    5. Promosi berjenjang hingga Grand Final: 3 pemenang Babak 3 masuk ke Grand Final, dan penetapan pemenang Grand Final menandai status turnamen tuntas.
  </action>
  <verify>Jalankan & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/bracket-3lane.test.js</verify>
  <done>File test suite tersedia untuk memandu dan memverifikasi implementasi TDD</done>
</task>

<task type="auto">
  <name>Task 2: Update Bracket Database Initial Seeding & Round Support in db.js</name>
  <files>server/db.js</files>
  <action>
    1. Di `server/db.js` pada fungsi `seedInitialData`:
       - Perbarui seeding bagan awal turnamen: ganti struktur kaku 2-peserta 8-slot lama menjadi struktur turnamen 3-jalur modern.
       - Buat 3 heat awal Babak 2 (`round_number = 2`, `match_number` 1, 2, 3) dan 1 heat Babak 3 / Grand Final (`round_number = 3`, `match_number` 4) sebagai parent match.
       - Pastikan backward compatibility dengan query yang memeriksa `user_id_1`, `user_id_2`, atau `user_id_3`.
  </action>
  <verify>Jalankan & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/race-flow.test.js</verify>
  <done>Database ter-seed dengan skema 3-jalur default yang bersih</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dynamic 3-Lane seedIntoBracket in raceManager.js</name>
  <files>server/raceManager.js</files>
  <action>
    1. Di `server/raceManager.js`:
       - Implementasikan `static seedIntoBracket(userId)`:
         a. Cek apakah `userId` sudah terdaftar di babak eliminasi pertama (`round_number = 2`) pada `user_id_1`, `user_id_2`, atau `user_id_3`. Jika sudah, return tanpa modifikasi.
         b. Cari heat Babak 2 (`round_number = 2`) yang masih memiliki slot kosong (`user_id_1 IS NULL OR user_id_2 IS NULL OR user_id_3 IS NULL`) dengan urutan `match_number ASC`.
         c. Isi slot kosong pertama: `user_id_1` jika kosong, else `user_id_2`, else `user_id_3`.
         d. Jika tidak ada heat dengan slot kosong di Babak 2, buat heat baru:
            - Cari `MAX(match_number)` di `bracket_matches`.
            - Insert heat baru dengan `id = uuidv4()`, `match_number = max + 1`, `round_number = 2`, `user_id_1 = userId`, `status = 'pending'`.
       - Jadikan `static placeIntoBracket(userId)` sebagai alias pemanggil `seedIntoBracket(userId)` agar integrasi scrutineer eksisting tetap berjalan mulus.
  </action>
  <verify>Jalankan & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/bracket-3lane.test.js</verify>
  <done>Kontestan Babak 1 yang lolos ter-seed otomatis ke slot 3-jalur Babak 2 dan heat baru ter-generate dinamis</done>
</task>

<task type="auto">
  <name>Task 4: Implement 3:1 Hierarchical advanceBracketWinner in raceManager.js</name>
  <files>server/raceManager.js</files>
  <action>
    1. Di `server/raceManager.js`:
       - Perbarui `static advanceBracketWinner(matchId, winnerId)`:
         a. Ambil data match dari `bracket_matches WHERE id = matchId`. Validasi bahwa `match` ada dan `winnerId` merupakan salah satu kontestan (`user_id_1`, `user_id_2`, atau `user_id_3`).
         b. Update match: `winner_id = winnerId, status = 'completed'`.
         c. Tentukan ronde tujuan: `nextRound = match.round_number + 1`.
         d. Cek apakah `winnerId` sudah terdaftar di `nextRound` (mencegah duplikasi promosi jika RD mengklik ulang).
         e. Jika `match.parent_match_id` terdefinisi dan parent match tersebut masih memiliki slot kosong (`!parent.user_id_1` atau `!parent.user_id_2` atau `!parent.user_id_3`), tempatkan `winnerId` ke slot kosong parent match tersebut.
         f. Jika `match.parent_match_id` tidak ada atau parent match sudah penuh:
            - Cari match di `nextRound` yang masih memiliki slot kosong terurut `match_number ASC`.
            - Jika ditemukan, isi slot kosong tersebut dan perbarui `parent_match_id` pada match saat ini.
            - Jika tidak ada match dengan slot kosong di `nextRound`, buat match baru di `nextRound`:
              - `id = uuidv4()`, `match_number = MAX(match_number) + 1`, `round_number = nextRound`, `user_id_1 = winnerId`, `status = 'pending'`.
              - Update `parent_match_id` pada match saat ini menunjuk ke match baru tersebut.
         g. Return `{ success: true, matchId, winnerId, nextRound }`.
  </action>
  <verify>Jalankan & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/bracket-3lane.test.js</verify>
  <done>Pemenang heat terpromosikan otomatis ke babak berikutnya dengan pola reduksi 3:1 hingga Grand Final</done>
</task>

<task type="auto">
  <name>Task 5: Run Full Verification & Regression Test Suite</name>
  <files>server/tests/bracket-3lane.test.js, server/tests/race-flow.test.js</files>
  <action>
    Jalankan kedua test suite untuk membuktikan:
    1. Seluruh skenario baru di `bracket-3lane.test.js` (3-lane slots, dynamic 100+ heats, auto-advance multi-round) lulus 100%.
    2. Alur balapan eksisting di `race-flow.test.js` (registrasi QR, antrian, scrutineer pass/fail, BTO calculation, cashier topup) tetap berjalan sempurna 100% tanpa regresi.
  </action>
  <verify>
    & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/bracket-3lane.test.js
    & "C:\Users\yudhiar\AppData\Local\nvm\v22.22.3\node.exe" server/tests/race-flow.test.js
  </verify>
  <done>Backend engine 3-lane elimination teruji dan tervalidasi secara komprehensif</done>
</task>
</tasks>
