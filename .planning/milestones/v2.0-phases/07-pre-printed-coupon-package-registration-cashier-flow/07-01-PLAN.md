---
phase: "07-pre-printed-coupon-package-registration-cashier-flow"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "server/db.js"
  - "server/index.js"
  - "server/tests/coupon-package.test.js"
  - "package.json"
autonomous: true
requirements:
  - "CPN-01"
  - "CPN-02"
  - "CPN-03"

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Tabel coupon_packages memiliki constraint UNIQUE(serial_number) dan kolom tracking kuota sequential (total_quota, used_quota, remaining_quota, status, void_from_id)."
    - "Pendaftaran paket kupon baru berhasil menambahkan data paket, mengaitkan atau membuat user on-the-fly, dan menyinkronkan saldo kupon di tabel coupons."
    - "Mencoba mendaftarkan nomor seri yang sudah ada menghasilkan error HTTP 409 Conflict beserta detail pemilik paket yang terdaftar."
    - "API void dan ganti lembar berhasil menonaktifkan lembar lama dan menerbitkan lembar seri baru dengan membawa sisa kuota yang belum terpakai."
    - "Pencarian paket kupon melalui query parameter ?search= dan ?status= mengembalikan data paket yang relevan dengan join nama pembalap dan tim."
  artifacts:
    - "server/db.js"
    - "server/index.js"
    - "server/tests/coupon-package.test.js"
  key_links:
    - "server/index.js -> server/db.js: Menggunakan db.transaction() untuk atomic registration dan balance sync."
    - "server/tests/coupon-package.test.js -> server/db.js: Test otomatis memverifikasi constraint keunikan serial dan integritas kuota."
---

<objective>
Mengimplementasikan skema database `coupon_packages` di SQLite WASM (`server/db.js`), menyediakan REST API endpoints untuk registrasi paket, validasi nomor seri unik, pencarian paket, sinkronisasi saldo kupon, serta alur darurat Void & Ganti Lembar di `server/index.js`, dan memverifikasinya melalui test suite otomatis komprehensif di `server/tests/coupon-package.test.js`.

Purpose: Membangun fondasi backend dan model data yang kokoh serta teruji untuk pengelolaan kupon fisik 50-kotak sebelum antarmuka kasir dan dasbor marshal diintegrasikan.
Output: Skema database baru, REST API routes, dan test suite otomatis yang 100% green.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pre-printed-coupon-package-registration-cashier-flow/07-CONTEXT.md
@.planning/phases/07-pre-printed-coupon-package-registration-cashier-flow/07-RESEARCH.md
@.planning/phases/07-pre-printed-coupon-package-registration-cashier-flow/07-VALIDATION.md
@server/db.js
@server/index.js
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Skema Database coupon_packages, Constraint Unik Serial, dan Test Suite Wave 0</name>
  <files>server/db.js, server/tests/coupon-package.test.js, package.json</files>
  <read_first>server/db.js, server/tests/race-flow.test.js, package.json</read_first>
  <behavior>
    - Test 1: Inisialisasi database membuat tabel `coupon_packages` dengan kolom: id, serial_number, user_id, total_quota, used_quota, remaining_quota, price_paid, payment_method, status, void_from_id, created_at, updated_at (per D-10).
    - Test 2: Insert paket kupon dengan nomor seri duplikat melempar exception keunikan SQLite (per D-03).
    - Test 3: Multiple packages untuk satu pembalap (user_id sama, serial_number berbeda) berhasil disimpan (per D-05).
  </behavior>
  <action>
    1. Buat test suite Wave 0 di `server/tests/coupon-package.test.js` menggunakan `node:assert` yang menguji inisialisasi tabel, insert valid, dan penolakan duplikasi serial.
    2. Tambahkan script test di `package.json` agar mencakup `node server/tests/coupon-package.test.js`.
    3. Di `server/db.js`, perbarui fungsi `initDatabase()` untuk membuat tabel `coupon_packages` dengan constraint:
       - `id TEXT PRIMARY KEY`
       - `serial_number TEXT UNIQUE NOT NULL` (per D-01, D-03)
       - `user_id TEXT NOT NULL`
       - `total_quota INTEGER NOT NULL DEFAULT 50` (per D-09)
       - `used_quota INTEGER NOT NULL DEFAULT 0` (per D-10)
       - `remaining_quota INTEGER NOT NULL DEFAULT 50` (per D-10)
       - `price_paid INTEGER DEFAULT 0` (per D-12)
       - `payment_method TEXT DEFAULT 'cash'` (per D-12)
       - `status TEXT CHECK(status IN ('active', 'completed', 'void')) DEFAULT 'active'` (per D-10, D-11)
       - `void_from_id TEXT DEFAULT NULL` (per D-08)
       - `created_at DATETIME DEFAULT CURRENT_TIMESTAMP`
       - `updated_at DATETIME DEFAULT CURRENT_TIMESTAMP`
       - `FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE`
       - `FOREIGN KEY(void_from_id) REFERENCES coupon_packages(id)`
    4. Pastikan migrasi aman jika database sudah ada dengan `CREATE TABLE IF NOT EXISTS` dan buat index `idx_coupon_packages_serial` pada `serial_number`.
  </action>
  <verify>
    <automated>node server/tests/coupon-package.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `server/db.js` contains `CREATE TABLE IF NOT EXISTS coupon_packages`
    - `server/db.js` contains `UNIQUE(serial_number)` or `CREATE UNIQUE INDEX`
    - `server/tests/coupon-package.test.js` exits with code 0
  </acceptance_criteria>
  <done>Tabel `coupon_packages` terbuat dengan schema dan unique index yang benar, dan test initial assert lulus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REST API Registrasi Paket Kupon, Dual Mode Racer, Sync Saldo Kupon, dan Search Endpoint</name>
  <files>server/index.js, server/tests/coupon-package.test.js</files>
  <read_first>server/index.js, server/db.js, server/tests/coupon-package.test.js</read_first>
  <behavior>
    - Test 1: POST `/api/coupon-packages` dengan `serial_number`, `user_id`, `total_quota` mengembalikan 201 Created dan menambah balance di tabel `coupons` sebesar `total_quota` (per D-07).
    - Test 2: POST `/api/coupon-packages` dengan `new_user_name` otomatis membuat user baru di `users` dan membuatkan record awal di `coupons` sebelum sync kuota (per D-06).
    - Test 3: POST `/api/coupon-packages` dengan `serial_number` yang sudah terdaftar mengembalikan 409 Conflict dengan payload data paket dan pemilik yang sudah terdaftar (per D-03).
    - Test 4: GET `/api/coupon-packages` dengan query parameter `search` memfilter berdasarkan nomor seri, nama pembalap, atau nama tim (per D-15).
    - Test 5: GET `/api/coupon-packages/next-serial` mengembalikan saran nomor seri berikutnya (misal jika serial tertinggi "042", kembalikan "043") (per D-04).
  </behavior>
  <action>
    1. Tambahkan endpoint `POST /api/coupon-packages` di `server/index.js`:
       - Validasi payload: `serial_number` (string 4–32 chars, trim, uppercase), `total_quota` (default 50, number > 0), `price_paid`, `payment_method`.
       - Pengecekan pra-insert untuk nomor seri yang sudah ada. Jika ada, kembalikan HTTP 409 dengan error code `SERIAL_EXISTS` dan data detail pemilik paket (`existing_package`, `racer_name`, `remaining_quota`) per D-03.
       - Eksekusi pendaftaran dalam `db.transaction()`: buat user jika dual-mode new user (per D-06), insert ke `coupon_packages` (per D-01, D-10), dan tambahkan `total_quota` ke saldo di tabel `coupons` (per D-07).
       - Broadcast event real-time melalui `io.emit('coupon_package_updated', { type: 'created', package: newPkg })`.
    2. Tambahkan endpoint `GET /api/coupon-packages`:
       - Join tabel `coupon_packages` dengan `users` untuk menyertakan `user_name`, `team_name`, dan email.
       - Dukung query parameter `?search=` (mencari di `serial_number`, `users.name`, `users.team_name`) dan `?status=` (active/completed/void) per D-15.
       - Urutkan descending berdasarkan `created_at`.
    3. Tambahkan endpoint `GET /api/coupon-packages/next-serial`:
       - Query nomor seri numerik tertinggi terakhir untuk memberikan saran auto-increment (per D-04).
    4. Perluas `server/tests/coupon-package.test.js` dengan integrasi test untuk semua route di atas.
  </action>
  <verify>
    <automated>node server/tests/coupon-package.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `POST /api/coupon-packages` returns 201 with `{ success: true, data: { package: ... } }`
    - `POST /api/coupon-packages` with duplicate serial returns 409 with `{ success: false, error: 'SERIAL_EXISTS', existing_package: ... }`
    - `GET /api/coupon-packages` returns `{ success: true, data: [...] }`
    - `GET /api/coupon-packages/next-serial` returns `{ success: true, data: { next_serial: ... } }`
  </acceptance_criteria>
  <done>Endpoint registrasi kupon, pengecekan duplikat 409, sync saldo, dan list search bekerja dengan sempurna dan terverifikasi oleh test otomatis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Alur Darurat Void & Transfer Kuota Lembar Rusak/Hilang</name>
  <files>server/index.js, server/tests/coupon-package.test.js</files>
  <read_first>server/index.js, server/db.js, server/tests/coupon-package.test.js</read_first>
  <behavior>
    - Test 1: POST `/api/coupon-packages/:id/void` dengan `new_serial_number` dan `reason` berhasil menonaktifkan paket lama (`status = 'void'`) dan membuat paket baru dengan `remaining_quota` yang sama (per D-08).
    - Test 2: Saldo user di tabel `coupons` TIDAK BERUBAH saat void & replace (karena sisa kuota hanya ditransfer ke lembar kertas fisik baru) (per D-08).
    - Test 3: Void ditolak dengan 400 jika paket lama berstatus 'completed' (kuota 0) atau sudah 'void'.
    - Test 4: Void ditolak dengan 409 jika `new_serial_number` sudah pernah digunakan di sistem.
  </behavior>
  <action>
    1. Implementasikan endpoint `POST /api/coupon-packages/:id/void` di `server/index.js`:
       - Ambil paket lama berdasarkan `id`. Verifikasi bahwa `status === 'active'` dan `remaining_quota > 0`.
       - Validasi `new_serial_number` (4–32 chars alfanumerik) dan pastikan belum ada di database.
       - Eksekusi transaksi atomik via `db.transaction()`:
         a. Update paket lama: `status = 'void'`, `updated_at = CURRENT_TIMESTAMP`.
         b. Insert paket baru: `serial_number = new_serial_number`, `user_id = old.user_id`, `total_quota = old.remaining_quota`, `used_quota = 0`, `remaining_quota = old.remaining_quota`, `status = 'active'`, `void_from_id = old.id`, `payment_method = 'transfer_void'`, `price_paid = 0`.
         c. JANGAN modifikasi saldo `coupons` pembalap karena sisa kuota fisik sudah tercermin dalam saldo digital pembalap.
       - Emit WebSocket event `io.emit('coupon_package_updated', { type: 'voided', old_id: id, new_package: newPkg })`.
    2. Lengkapi test suite di `server/tests/coupon-package.test.js` untuk memverifikasi skenario void transfer kuota, penolakan serial baru yang duplikat, dan invariansi saldo digital pembalap.
  </action>
  <verify>
    <automated>node server/tests/coupon-package.test.js</automated>
    <fails_when>non-zero exit or AssertionError in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `POST /api/coupon-packages/:id/void` returns 200 with `{ success: true, data: { new_package: ... } }`
    - Target package in DB has status 'void'
    - New package in DB has status 'active' and remaining_quota equals old package's remaining_quota
    - User's coupons balance remains unchanged
  </acceptance_criteria>
  <done>Fitur void dan transfer kuota lembar rusak/hilang berfungsi secara atomik, aman dari duplikasi, dan saldo pembalap tetap konsisten.</done>
</task>

</tasks>

## Artifacts this phase produces
- SQL Table: `coupon_packages` in `server/db.js`
- API Route: `POST /api/coupon-packages` in `server/index.js`
- API Route: `GET /api/coupon-packages` in `server/index.js`
- API Route: `GET /api/coupon-packages/next-serial` in `server/index.js`
- API Route: `POST /api/coupon-packages/:id/void` in `server/index.js`
- Test File: `server/tests/coupon-package.test.js`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cashier Input → API Endpoints | Input nomor seri, nama pembalap, dan nominal harga dari formulir web kasir melewati batas API Express. |
| API Endpoints → SQLite Storage | Query SQL prepared statement mengeksekusi manipulasi data paket kupon dan saldo pembalap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `server/db.js` `coupon_packages` | high | mitigate | Terapkan constraint `UNIQUE(serial_number)` di level schema database untuk mencegah pendaftaran ganda di seluruh turnamen (per D-03). |
| T-07-02 | Tampering | `server/index.js` `POST /api/coupon-packages` | high | mitigate | Gunakan `db.transaction()` untuk pendaftaran paket dan sync saldo `coupons` agar tidak terjadi penambahan saldo jika insert paket gagal. |
| T-07-03 | Tampering | `server/index.js` `POST /api/coupon-packages/:id/void` | medium | mitigate | Validasi status paket asal (`active` dan `remaining_quota > 0`) dan verifikasi keunikan nomor seri baru sebelum transfer kuota dilakukan. |
| T-07-04 | Denial of Service | `server/index.js` `GET /api/coupon-packages` | low | mitigate | Batasi panjang input search parameter dan sediakan limit default 100 record jika daftar paket bertambah besar. |
</threat_model>

<verification>
Automated:
1. `node server/tests/coupon-package.test.js` - Menguji seluruh siklus hidup skema paket kupon, constraint unik, API CRUD, sync saldo, dan void transfer.
2. `npm test` - Memastikan test suite eksisting tidak mengalami regresi.
</verification>

<success_criteria>
1. Tabel `coupon_packages` dibuat dengan constraint `UNIQUE(serial_number)` dan kolom pelacakan kuota sequential.
2. Endpoint `POST /api/coupon-packages` mendaftarkan paket, menolak nomor duplikat dengan 409 Conflict, dan menyinkronkan saldo ke tabel `coupons`.
3. Endpoint `POST /api/coupon-packages/:id/void` menonaktifkan lembar rusak dan mentransfer sisa kuota ke lembar baru secara atomik.
4. Seluruh test otomatis di `server/tests/coupon-package.test.js` berstatus pass.
</success_criteria>

<output>
Create `.planning/phases/07-pre-printed-coupon-package-registration-cashier-flow/07-01-SUMMARY.md` when done
</output>
