---
phase: 02-scrutineer-desk-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/raceManager.js
  - server/index.js
  - client/src/context/RaceContext.jsx
  - client/src/screens/ScrutineerDashboard.jsx
  - server/tests/race-flow.test.js
autonomous: true
requirements:
  - SCRUT-01
  - SCRUT-02
must_haves:
  truths:
    - "Scrutineer tablet displays Active Alert (Lapis 2) flashing yellow banner 'Race Aktif Belum Disubmit Admin' when an active heat is locked or in progress but queue is empty"
    - "Scrutineer tablet provides an emergency 'Ambil Alih Hasil (Override)' modal dialog (Lapis 3) allowing the scrutineer to select the winning lane, input optional finish time, and immediately pass or DQ the winner"
    - "Emergency override sets winner in database, updates race status to completed, checks BTO record, and auto-seeds winner into Round 2 bracket tree"
    - "Automated tests verify scrutineer emergency override flow end-to-end"
  artifacts:
    - server/raceManager.js
    - server/index.js
    - client/src/screens/ScrutineerDashboard.jsx
    - server/tests/race-flow.test.js
---

<objective>
Implement Scrutineer Desk Alerts & Emergency Override:
1. Active Alert Lapis 2 (SCRUT-01) - Flashing banner when cars are on track or finished but Race Director hasn't submitted finish times.
2. Emergency Override Lapis 3 (SCRUT-02) - Complete fail-safe mechanism allowing Scrutineer to select winner and declare Pass/DQ directly from the inspection desk.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement Backend Logic for Scrutineer Override</name>
  <files>server/raceManager.js, server/index.js</files>
  <action>
    1. In server/raceManager.js:
       - Add `scrutineerOverride({ raceId, lane, finishTime, action })`:
         - Verifies race exists and is in 'pre-start' or 'locked' status.
         - Finds registration for the specified lane.
         - If finishTime provided, updates registration finish_time; else sets a fallback time (e.g. 0.000 or manual time).
         - Sets winner_id = reg.user_id on race.
         - Calls `handleScrutineerAction(reg.id, action || 'pass')`.
         - Returns the result including BTO check and bracket auto-placement.
    2. In server/index.js:
       - Add POST `/api/race/scrutineer-override`:
         - Calls `RaceManager.scrutineerOverride(req.body)`.
         - Emits `STATE_UPDATE` to all clients.
         - If pass and new BTO, emits `NEW_BTO_RECORD`.
         - Returns JSON response.
  </action>
  <verify>Run node server/tests/race-flow.test.js</verify>
  <done>Backend endpoint and method execute atomic override and auto-placement</done>
</task>

<task type="auto">
  <name>Task 2: Implement UI Alerts and Override Modal in ScrutineerDashboard</name>
  <files>client/src/context/RaceContext.jsx, client/src/screens/ScrutineerDashboard.jsx</files>
  <action>
    1. In client/src/context/RaceContext.jsx:
       - Add `apiScrutineerOverride({ raceId, lane, finishTime, action })` helper function.
       - Export in RaceContext value.
    2. In client/src/screens/ScrutineerDashboard.jsx:
       - Detect when `queue.length === 0` AND activeRace status is 'locked' or 'pre-start':
         - Render Lapis 2 Active Alert: flashing amber CyberCard with AlertTriangle:
           - Title: "PERINGATAN: RACE AKTIF BELUM DISUBMIT ADMIN (HEAT #[X])"
           - Subtitle: "Pembalap mungkin sudah tiba di meja inspeksi namun Race Director belum menginput hasil waktu dari layar trek."
           - Action button: "AMBIL ALIH HASIL (OVERRIDE DARI MEJA JURI)"
       - Implement Scrutineer Override Modal Dialog:
         - Select winner lane card (Jalur A, B, or C with racer name and tag).
         - Input finish time (number input with step 0.001, e.g. 11.450).
         - Two primary action buttons:
           - "LOLOSKAN (PASS) & MASUKKAN KE BAGAN"
           - "DISKUALIFIKASI (DQ)"
         - Submits to `apiScrutineerOverride` and gives instant visual feedback.
  </action>
  <verify>Run npm --prefix client run build to verify zero syntax/bundling errors</verify>
  <done>Scrutineer tablet has working Lapis 2 alert and Lapis 3 emergency override modal</done>
</task>

<task type="auto">
  <name>Task 3: Automated Testing & Verification</name>
  <files>server/tests/race-flow.test.js</files>
  <action>
    Extend server/tests/race-flow.test.js with test step 12:
    - Register racers on a heat and lock it.
    - Call `scrutineerOverride` directly from scrutineer desk on Lane B with time 10.850s and action 'pass'.
    - Assert race is 'completed', winner_id is user in Lane B, finish_time is 10.850, scrutineer_status is 'pass', and user is auto-placed into Round 2 bracket tree.
  </action>
  <verify>node server/tests/race-flow.test.js passes with 100% assertions</verify>
  <done>12/12 automated test steps pass without error</done>
</task>
</tasks>
