---
phase: "12"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - server/utils/csvParser.js
  - server/services/participantService.js
  - server/index.js
  - scripts/import-roster.js
  - server/tests/participant-registration.test.js
  - package.json
autonomous: true
requirements: [PARN-01, PARN-02, PARN-03, PARN-05, EVNT-03]
user_setup: []

must_haves:
  truths:
    - "A participant can be registered with racer name and optional team name without requiring coupon packages or balances (PARN-01, D-05)"
    - "System allocates sequential participant_number starting from 1 for a new event and continuing sequentially within db.transaction() (PARN-02, EVNT-03, D-09)"
    - "Multi-entry with identical racer names is permitted without unique constraint collision by storing email = NULL (D-04)"
    - "Omni-search filters by participant number when input is numeric, or by name and team when input is textual (PARN-03, D-06)"
    - "Participant typo updates (name and team) do not alter the existing participant_number or event_id (D-12)"
    - "CSV roster parser handles standard headers (name/nama, team/tim), STC Vol 8 format, and headerless fallback (PARN-05, D-08)"
    - "REST endpoints /api/participants, /api/participants/import-preview, /api/participants/import, and /api/participants/:id are active and emit WebSocket events"
    - "CLI script scripts/import-roster.js registers participants into the active event with auto-numbering (PARN-05, D-07)"
    - "Automated test suite server/tests/participant-registration.test.js passes all 10 test cases and is registered in package.json test script"
  artifacts:
    - path: "server/utils/csvParser.js"
      provides: "RFC4180-compliant tokenizeCsv() and flexible parseParticipantCsv() supporting standard and STC Vol 8 formats"
    - path: "server/services/participantService.js"
      provides: "Domain service: registerParticipant, getParticipants, updateParticipant, importParticipants"
    - path: "server/index.js"
      provides: "REST endpoints /api/participants* with WebSocket notifications"
    - path: "scripts/import-roster.js"
      provides: "CLI script supporting batch participant roster import into active event with auto-numbering"
    - path: "server/tests/participant-registration.test.js"
      provides: "Comprehensive Node.js assert test suite covering T-PARN-01 through T-PARN-10"
  key_links:
    - from: "server/services/participantService.js"
      to: "server/db.js"
      via: "db.transaction() for atomic MAX(participant_number) + 1 allocation"
      pattern: "db\\.transaction"
    - from: "server/services/participantService.js"
      to: "server/services/eventService.js"
      via: "getActiveEventId() to scope participant allocation to active event"
      pattern: "getActiveEventId"
    - from: "server/index.js"
      to: "server/services/participantService.js"
      via: "Route handlers calling registerParticipant, getParticipants, updateParticipant, importParticipants"
      pattern: "participantService"
---

<objective>
Implement the complete backend domain service, CSV parser, REST API endpoints, WebSocket notifications, CLI import script, and 10-point automated test suite for participant registration and auto-numbering.

Purpose: Provide a race-condition-free, event-scoped participant auto-numbering engine (`#1`, `#2`, `#3`...) for v3.0 physical racing that enables instant multi-entry registrations, typo corrections, omni-search, and CSV roster imports.
Output: `server/utils/csvParser.js`, `server/services/participantService.js`, updated `server/index.js`, updated `scripts/import-roster.js`, and `server/tests/participant-registration.test.js` wired into `package.json`.
Citations: Fulfills PARN-01, PARN-02, PARN-03, PARN-05, EVNT-03 and implements D-04, D-05, D-06, D-07, D-08, D-09, D-12.
</objective>

<execution_context>
@C:/Users/yudhiar/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/yudhiar/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-participant-registration-auto-numbering/12-CONTEXT.md
@.planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md
@.planning/phases/12-participant-registration-auto-numbering/12-VALIDATION.md
@server/db.js
@server/services/eventService.js
@server/utils/participantNumber.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CSV Parser Utility and Participant Domain Service</name>
  <files>server/utils/csvParser.js, server/services/participantService.js</files>
  <read_first>
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 7.1 "CSV Parser Utility: server/utils/csvParser.js"
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 7.2 "Domain Service: server/services/participantService.js"
    - server/services/eventService.js
    - server/utils/participantNumber.js
  </read_first>
  <behavior>
    - parseParticipantCsv parses standard CSV with headers 'name,team' or 'nama,tim' into { valid, skipped }
    - parseParticipantCsv parses STC Vol 8 format detecting 'nama racer' and filtering 'Lunas'/comp entries
    - parseParticipantCsv falls back to headerless format (col 0 = name, col 1 = team)
    - registerParticipant validates non-empty name (max 100 chars) and trims optional team name (max 50 chars, null if empty) per D-05
    - registerParticipant requires active event (throws if no active event per EVNT-03)
    - registerParticipant assigns sequential participant_number starting at 1 per event using db.transaction() per D-09, EVNT-03
    - registerParticipant permits identical racer names (multi-entry) by setting email = NULL per D-04
    - getParticipants returns list ordered by participant_number DESC with total, latest_number, and active_event
    - getParticipants omni-search supports numeric participant_number (#42 or 42) and text substring matching for name and team per D-06
    - updateParticipant allows editing name and team_name, but protects participant_number and event_id from modification per D-12
    - importParticipants sequentially allocates participant_number within a single transaction continuing from MAX(participant_number) + 1 per D-09
  </behavior>
  <acceptance_criteria>
    - `server/utils/csvParser.js` exports `tokenizeCsv(text)` and `parseParticipantCsv(csvText)`.
    - `server/services/participantService.js` exports `registerParticipant`, `getParticipants`, `updateParticipant`, `importParticipants`.
    - `registerParticipant` allocates `COALESCE(MAX(participant_number), 0) + 1` inside `db.transaction()`.
    - `updateParticipant` mutates only `name` and `team_name` in `users`.
  </acceptance_criteria>
  <action>
    Create `server/utils/csvParser.js` per RESEARCH § 7.1:
    - Implement `tokenizeCsv(text)` supporting RFC4180 quotes, escaped quotes `""`, commas, and newlines (`\r\n` and `\n`) without external dependencies.
    - Implement `parseParticipantCsv(csvText)` per D-08: inspect first 5 rows to detect standard headers (`name`, `nama`, `pembalap`, `racer`, and `team`, `tim`, `klub`, `club`, `tag`) or STC Vol 8 format (`nama racer`, `kupon sales`, checking status `Lunas` or comp `rp0`). Provide headerless fallback. Return `{ valid: [{ name, team_name }], skipped: [{ row, reason }] }`.

    Create `server/services/participantService.js` per RESEARCH § 7.2:
    - Implement `registerParticipant({ name, team_name, event_id })`: validate name (trim, required, <= 100 chars), sanitize team_name (trim, <= 50 chars, null if empty per D-05). Resolve targetEventId via `event_id || getActiveEventId()`; throw Error if null per EVNT-03. In `db.transaction()`, query `SELECT COALESCE(MAX(participant_number), 0) AS max_num FROM users WHERE event_id = ?`, compute `nextNum = max_num + 1`, insert row into `users` with `id = uuidv4()`, `email = NULL` (to avoid unique constraint violations on multi-entry per D-04), `role = 'participant'`, `is_virtual = 0`, `event_id = targetEventId`, `participant_number = nextNum`. Return created record.
    - Implement `getParticipants({ event_id, search, limit = 200, offset = 0 })`: resolve targetEventId; return empty structure if no active event. Query `COUNT(*)` as `total` and `COALESCE(MAX(participant_number), 0)` as `latest_number` for `role = 'participant'`. If `search` provided, normalize via `normalizeParticipantNumber(search.replace(/^#/, ''))`: if numeric, query `(participant_number = ? OR name LIKE ? OR team_name LIKE ?)` per D-06; otherwise search `(name LIKE ? OR team_name LIKE ?)`. Order by `participant_number DESC LIMIT ? OFFSET ?`. Include active event object from `getActiveEvent()`.
    - Implement `updateParticipant(id, { name, team_name })` per D-12: validate name is non-empty. In `db.transaction()`, check if user exists; execute `UPDATE users SET name = ?, team_name = ? WHERE id = ?`. Do NOT allow updating `participant_number` or `event_id`. Return updated record.
    - Implement `importParticipants(participantsList, { event_id })` per D-09: validate array is non-empty. Resolve event. In `db.transaction()`, retrieve current max `participant_number`, then loop over participants allocating contiguous `nextNum = currentMax + idx + 1`, inserting each into `users` with `email = NULL`, `role = 'participant'`. Return `{ count, imported }`.
  </action>
  <verify>
    <automated>node -e "import('./server/utils/csvParser.js').then(m => console.log('csvParser ok')); import('./server/services/participantService.js').then(m => console.log('participantService ok'));"</automated>
    <fails_when>Module import fails or syntax errors exist</fails_when>
  </verify>
  <done>CSV parser and participant domain services are created with complete transaction and scoping logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement REST API Endpoints, WebSocket Events, and CLI Roster Script</name>
  <files>server/index.js, scripts/import-roster.js</files>
  <read_first>
    - server/index.js
    - scripts/import-roster.js
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 3.3 "REST API Endpoints" & § 3.4 "WebSocket Broadcasts"
  </read_first>
  <behavior>
    - POST /api/participants registers participant, emits 'participant_registered' and broadcasts state, returns 201
    - GET /api/participants returns { success: true, data: { active_event, total, latest_number, participants } }
    - PUT /api/participants/:id updates name/team, emits 'participant_updated' and broadcasts state, returns 200
    - POST /api/participants/import-preview accepts CSV text or { csv } body, returns preview { valid, skipped }
    - POST /api/participants/import accepts { participants: [...] } or { csv: "..." }, inserts batch, emits 'participants_imported' and broadcasts state, returns 201
    - scripts/import-roster.js runs via CLI: supports --csv, --dry, parses using parseParticipantCsv and imports participants into active event
  </behavior>
  <acceptance_criteria>
    - `server/index.js` registers `/api/participants`, `/api/participants/import-preview`, `/api/participants/import`, and `/api/participants/:id`.
    - Endpoints emit `participant_registered`, `participant_updated`, `participants_imported` on `io`.
    - `scripts/import-roster.js` supports participant roster import into active event without creating legacy coupon packages or balances.
  </acceptance_criteria>
  <action>
    In `server/index.js`:
    - Import `registerParticipant`, `getParticipants`, `updateParticipant`, `importParticipants` from `./services/participantService.js` and `parseParticipantCsv` from `./utils/csvParser.js`.
    - Add endpoint `POST /api/participants` (PARN-01, PARN-02):
      Extract `{ name, team_name, event_id } = req.body || {}`.
      Call `registerParticipant({ name, team_name, event_id })`.
      Emit `io.emit('participant_registered', participant)` and `broadcastFullState()`.
      Return `res.status(201).json({ success: true, data: participant })`. Catch errors with status 400.
    - Add endpoint `GET /api/participants` (PARN-03, D-06):
      Extract `event_id`, `search`, `limit`, `offset` from `req.query`.
      Call `getParticipants({ event_id, search, limit: parseInt(limit) || 200, offset: parseInt(offset) || 0 })`.
      Return `res.json({ success: true, data: result })`.
    - Add endpoint `PUT /api/participants/:id` (D-12):
      Extract `{ name, team_name } = req.body || {}`.
      Call `updateParticipant(req.params.id, { name, team_name })`.
      Emit `io.emit('participant_updated', updated)` and `broadcastFullState()`.
      Return `res.json({ success: true, data: updated })`. Catch errors with status 400 or 404.
    - Add endpoint `POST /api/participants/import-preview` (D-07, D-08):
      Extract CSV text from `req.body.csv` or raw body. Call `parseParticipantCsv(csvText)`.
      Return `res.json({ success: true, data: { valid: preview.valid, skipped: preview.skipped, totalRows: preview.valid.length + preview.skipped.length } })`.
    - Add endpoint `POST /api/participants/import` (D-07, D-09, PARN-05):
      Accept `{ participants, csv, event_id }`. If `csv` passed, run `parseParticipantCsv` first to get valid list.
      Call `importParticipants(participantsList, { event_id })`.
      Emit `io.emit('participants_imported', { count: result.count, event_id })` and `broadcastFullState()`.
      Return `res.status(201).json({ success: true, data: result })`.

    Update `scripts/import-roster.js` per D-07, D-08, D-09:
    - Adapt the script to import participants into the active event.
    - Parse CLI flags: `--csv <path>`, `--db <path>`, `--dry`, `--event <id>`.
    - Read CSV text, execute `parseParticipantCsv(csvText)`.
    - If `--dry`: log parsed valid participants count and skipped count with reasons, then exit 0.
    - If not `--dry`: call `initDatabase()`, resolve active event ID (or use `--event`), execute `importParticipants(preview.valid, { event_id })`.
    - Print formatted console summary of imported participants with allocated numbers (e.g. `#1` through `#N`).
  </action>
  <verify>
    <automated>node scripts/import-roster.js --dry</automated>
    <fails_when>Script fails to parse data/stc-vol8-roster.csv or crashes in dry-run mode</fails_when>
  </verify>
  <done>REST endpoints and CLI script are implemented and functional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author Comprehensive Automated Test Suite and Register in package.json</name>
  <files>server/tests/participant-registration.test.js, package.json</files>
  <read_first>
    - .planning/phases/12-participant-registration-auto-numbering/12-RESEARCH.md § 8.1 "Automated Test Matrix"
    - server/tests/migration-foundation.test.js
    - package.json
  </read_first>
  <behavior>
    - T-PARN-01: Participant registers with name/team, gets role='participant', email=NULL, coupon_balance=0
    - T-PARN-02: Sequential participant numbers #1, #2, #3 allocated in ascending order within same event
    - T-EVNT-03: Switching/activating a new event resets participant number to #1
    - T-PARN-03A: Omni-search finds participant by exact number '42' or '#42'
    - T-PARN-03B: Omni-search finds participant by substring in name or team
    - T-PARN-04: Multi-entry with identical racer name creates two different sequential numbers without collision
    - T-PARN-05: Empty team_name stores NULL in database and outputs null in API
    - T-PARN-06: Typo update via PUT changes name and team, but leaves participant_number and event_id intact
    - T-PARN-07: CSV import preview and execution adds participants continuing sequentially from MAX(participant_number) + 1
    - T-PARN-08: CSV parser handles STC Vol 8 format filtering Lunas/comp entries
    - T-PARN-09: Registering participant when no active event exists throws/returns 400 Bad Request
    - T-PARN-10: CLI script scripts/import-roster.js allocates numbers into active event without errors
  </behavior>
  <acceptance_criteria>
    - `server/tests/participant-registration.test.js` tests all 10 scenarios (T-PARN-01 through T-PARN-10).
    - `package.json` test script includes `node server/tests/participant-registration.test.js`.
    - Both `node server/tests/participant-registration.test.js` and `npm test` exit code 0.
  </acceptance_criteria>
  <action>
    Create `server/tests/participant-registration.test.js` using Node.js native `assert` runner:
    - Set up isolated temp database `DB_PATH = path.join(repoRoot, 'data', 'temp-test-participant-reg.sqlite')`.
    - Boot database with `await initDatabase()`.
    - Implement T-PARN-01: Register single participant without coupons, verify `role === 'participant'`, `email === null`, `participant_number === 1`.
    - Implement T-PARN-02: Register multiple participants consecutively, verify sequential numbers 1, 2, 3.
    - Implement T-EVNT-03: Create a new event via `createEvent()`, activate via `setActiveEvent()`, register participant, assert `participant_number === 1` (reset verified). Switch back to original event, register participant, assert numbering resumes at 4 (event isolation verified).
    - Implement T-PARN-03A & T-PARN-03B: Test `getParticipants` omni-search with numeric `#2` and `2`, and text substring searches.
    - Implement T-PARN-04: Register duplicate name `Budi Santoso` twice; assert both succeed and receive distinct sequential numbers (multi-entry verified per D-04).
    - Implement T-PARN-05: Register with missing/empty team, assert `team_name === null` per D-05.
    - Implement T-PARN-06: Call `updateParticipant`, assert `name` and `team_name` updated while `participant_number` and `event_id` remain identical per D-12.
    - Implement T-PARN-07 & T-PARN-08: Call `parseParticipantCsv` with standard CSV and STC Vol 8 CSV string, assert valid and skipped rows count. Call `importParticipants`, assert contiguous auto-sequence continuing from current max per D-09.
    - Implement T-PARN-09: Archive active event so no active event exists, call `registerParticipant`, assert error thrown with message mentioning no active event. Reactivate event.
    - Implement T-PARN-10: Execute CLI script `scripts/import-roster.js` via `child_process.execSync` against the temp DB; verify exit 0 and imported participants present.
    - Teardown: unlink temp DB in `finally` block.

    Update `package.json`:
    - In `"scripts"`, update `"test"` to append `&& node server/tests/participant-registration.test.js`.
  </action>
  <verify>
    <automated>node server/tests/participant-registration.test.js</automated>
    <fails_when>Any assertion fails in participant-registration.test.js or test throws an unhandled error</fails_when>
  </verify>
  <done>All 10 test cases pass and full test suite passes with zero regressions.</done>
</task>

</tasks>
