---
phase: "20"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - server/db.js
  - server/services/authService.js
  - server/index.js
  - server/tests/auth-service.test.js
  - package.json
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04, APPR-01]
user_setup: []

must_haves:
  truths:
    - "Database schema includes app_users and auth_sessions tables with proper constraints (D-01)"
    - "Logging in with tropicans@gmail.com automatically grants super_admin role and approved status (D-02, AUTH-03)"
    - "Logging in with any other Google email creates user with pending status and pending role (D-03, APPR-01)"
    - "Backend verifies Google ID tokens safely using OAuth2Client and issues secure session tokens (D-04, AUTH-02, AUTH-04)"
    - "Auth endpoints POST /api/auth/google, GET /api/auth/me, and POST /api/auth/logout handle authentication flow cleanly (D-05)"
    - "Automated test suite server/tests/auth-service.test.js passes 100% green without regressions"
  artifacts:
    - path: "server/db.js"
      provides: "Table migrations for app_users and auth_sessions"
    - path: "server/services/authService.js"
      provides: "Google token verification, user provisioning, and session management"
    - path: "server/index.js"
      provides: "REST API routes for Google login, current user info, and logout"
    - path: "server/tests/auth-service.test.js"
      provides: "Automated test suite covering token validation, admin auto-approval, pending user creation, and session lifecycle"
---

<objective>
Implement SQLite database tables for users and auth sessions, Google ID Token verification service, auto-super-admin provisioning for tropicans@gmail.com, pending user creation for other Google accounts, session token issuance, and auth REST endpoints.

Purpose: Provide the backend authentication infrastructure required for Google OAuth Sign-In and Admin Approval workflows.
Output: Schema in `server/db.js`, service in `server/services/authService.js`, endpoints in `server/index.js`, tests in `server/tests/auth-service.test.js`.
</objective>

<tasks>

<task type="auto" id="20-01-01">
<name>Install google-auth-library and update package.json</name>
<description>
Install `google-auth-library` dependency for verifying Google ID Tokens on Node.js backend.
</description>
</task>

<task type="auto" id="20-01-02">
<name>Database schema migration for app_users and auth_sessions</name>
<description>
Add migration in `server/db.js` creating `app_users` and `auth_sessions` tables if not exists, along with necessary indexes.
</description>
</task>

<task type="auto" id="20-01-03">
<name>Create authService.js with Google ID Token verification and user provisioning</name>
<description>
Implement `verifyGoogleToken`, `authenticateGoogleUser`, `getUserByToken`, and `invalidateSession` functions with auto-super-admin logic for `tropicans@gmail.com` and pending status for other accounts.
</description>
</task>

<task type="auto" id="20-01-04">
<name>Expose Auth REST API Endpoints in server/index.js</name>
<description>
Mount `POST /api/auth/google`, `GET /api/auth/me`, and `POST /api/auth/logout`.
</description>
</task>

<task type="auto" id="20-01-05">
<name>Create server/tests/auth-service.test.js and wire into npm test</name>
<description>
Write thorough automated test suite testing all authentication flows and verify 100% green pass with `npm test`.
</description>
</task>

</tasks>
