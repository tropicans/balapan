# Phase 23 Plan 01: Admin Approval Dashboard UI & Role-Based UI Controls

**Phase:** 23  
**Milestone:** v3.2 Google OAuth Authentication & Admin Approval System  
**Plan:** 23-01-PLAN.md  
**Status:** In Progress  

---

## 1. Goal
Complete the frontend implementation of the User Management & Approval Dashboard (`/admin`), dynamic role-scoped navigation in the top Navbar, real-time Socket.IO synchronization on user updates, and full end-to-end verification.

---

## 2. Tasks & Proposed Changes

### Task 1: Polish `client/src/screens/AdminUserDashboard.jsx`
- Add search filter bar (filtering by name or email).
- Connect Socket.IO listener for `user:updated` to auto-fetch users.
- Add quick stats counters (Total Users, Pending Requests, Approved Staff, Suspended).
- Polish Role Selection Modal with clear descriptions for each operational capability.
- Display Super Admin protection badge (`IMMUNE`) for `tropicans@gmail.com`.

### Task 2: Polish `client/src/components/ui/Navbar.jsx`
- Ensure dynamic nav tabs are visible only to users with appropriate roles (e.g. `/admin` only for `super_admin` / `admin`).
- Ensure active screen state reflects selected tab correctly.

### Task 3: Client Build & End-to-End Test Suite
- Run `npm --prefix client run build` to ensure clean build.
- Run `npm test` across all 19 test suites.

---

## 3. Verification Plan
- Build passes with zero errors.
- Test suites pass 100% green.
- Manual test script verifies admin can approve a pending user and assign role.
